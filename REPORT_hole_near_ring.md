# 报表：焊环与孔洞近乎相连工况下的「孔位」识别失效

> 2026-09-18 · 分析对象：`tests/pp` 现场 20MP 语料，用户标注的 Img000 / Img009 两帧为真值
> 结论一句话：**这两帧不是"焊环画歪了"，而是"孔位测错了"——分层定位在冲白面上整体塌掉后，
> 程序无条件退到"以焊环圆心为原点"的梯度卡尺，卡尺量到的其实是被冲白区的边界（半圈孔沿 + 半圈熔核内沿），
> 于是偏移量要么被静默压低 2.5 倍，要么被半径闸门拦下报成"异常"。**
>
> **测量时点说明（重要）**：本报表的分析数字在 `2026-09-18 14:20` 用当时的源码树复核过一遍；
> 之后的**修复实施与验证**记录在第 8~10 节。本次分析期间源码被并发修改过——`WeldRingLocator.cs`
> 与 `WeldInspectionAlgorithm.cs` 于 **13:04** 被改动（焊环侧），`InspectionConfig.cs` 于 11:16 被改动。
> **孔位侧的两个文件当时未变**（`HoleLocator.cs` 9/17 20:02、`PinLocator.cs` 更早），
> 因此本报表的根因结论（RC-1~RC-3）不受那次改动影响。本仓库不是 git 仓库，无法给出提交号，
> 故以文件时间为准。
>
> **一处事实修正（用户 2026-09-18 确认）**：`tests/pp` 里**一个 SN 目录 = 一条线上的 24 个零件**，
> 不是同一零件的重复拍摄。因此"同一 SN 内偏移量跨帧跨度 0.15~5.4mm"是**真实的零件间差异**，
> 不是测量误差；本报表早期的"刚性不变量"推断（RC-4）据此作废，误差判定全部改用
> **用户标注的绝对真值**（见 §1.2、§10）。

---

## 0. 摘要（先看这一节）

| 项 | 实测 |
|---|---|
| 用户报的两帧，孔位真值（用户手绘蓝圈反解到原图） | Img000 `(2389,1792) r=143`；Img009 `(2740,1918) r=114` |
| 算法报的孔位 | Img000 `(2642,1712) r=170`（圆心偏 **6.21mm**）；Img009 `(2649,1914) r=195`（半径 **1.7 倍**） |
| 偏移量 | Img000 报 `1.761mm`，真值 `4.45mm` → **偏低 2.5 倍**，界面仍报 OK；Img009 真值 `2.00mm`，算法报**异常**（无读数） |
| 触发路径（反射调用内部类取证） | 两帧都走 `HoleLocator → null → PinLocator（卡尺兜底）` |
| 分层为什么返回 null | 中间层只占搜索区 **4.7% / 3.6%**，3×3 开运算后**无任何面积 ≥60px 的连通域** |
| 影响面（40 SN / 933 可测帧） | 卡尺兜底 **72 帧 = 7.7%**；端到端"异常" **4 帧 = 0.4%，100% 来自卡尺兜底** |
| 两条路径的孔/环比 | 分层 `中位 0.226`（p95 0.249） vs 卡尺 `中位 0.306`（p95 0.367，max 0.400）→ 卡尺的"孔"平均大 35% |
| 同一 SN 内偏移量跨帧跨度 | 0.15~5.4mm —— 用户确认为**线上 24 个不同零件**，故这是零件间真实差异，**不是误差** |
| 已验证的修法方向 | 分层换成**三类 Otsu + 去掉开运算**：两帧孔心误差由 6.21/2.14mm 降到 **0.55/1.38mm**（原型实测） |
| **实施结果（§10）** | 两帧均改判分层：Img000 孔心差 **0.46mm**、偏移量 1.761→**4.587mm**（真值≈4.45）；Img009 不再报异常、**D=2.069mm**（真值 2.00）。全量：分层 861→**913 帧**（92.3%→97.9%），卡尺兜底 72→**20 帧**；**零回归**（原本走分层的 435 帧逐帧完全一致） |

---

## 1. 现象与真值

### 1.1 用户标注

- **Img000**（`19-22-06 113_Scene1Pos0_index1_SN_CCSAGZZG01A2536925090900002025080700034_OK_255.jpg`）：
  熔核左下方那块**带车削纹理的灰盘**才是孔位（用户蓝圈）；算法把绿圈画在熔核内部**过曝冲白区**的边界上。
- **Img009**（`16-49-03 116_Scene1Pos0_index1_SN_CCSAGZZG01A2536925090900003025080700034_OK_960.jpg`）：
  同上，且该帧被判为 `异常：孔洞半径异常（43.5%×焊环半径，期望 10%~40%）`。

### 1.2 真值获取（不依赖人工再读图）

沿用 FIXPLAN §4.1 的做法：把用户截图里的手绘圈按颜色分离，再用**算法自己画的红圈（焊环）/绿圈（极柱）**反解
"原图坐标 → 截图坐标"的等比+平移映射，然后作用到用户手绘圈上。

- 映射校验：红圈拟合半径 `91.11` vs 预期 `90.14`，绿圈 `32.09` vs `31.80`（误差 <1.2%），映射可信。
- Img000 蓝圈 → 原图 `(2389.4, 1791.7) r=143.3`（`= 0.297 × 焊环半径`）
- Img009 蓝圈 → 原图 `(2739.9, 1917.9) r=113.6`（`= 0.254 × 焊环半径`）
- 交叉校验：用户带卡尺实测过「孔 5mm、凹陷圆环 30mm 且二者同心」。Img009 的蓝圈 `r=113.6px = 2.67mm`
  （直径 5.3mm）与卡尺值吻合；Img000 的蓝圈略大（`3.36mm`），因为它把孔沿外的暗缝隙一起圈了进去，
  而灰盘自身的边界目视约 `r≈100px = 2.35mm`（直径 4.7mm）——**也吻合 5mm**。

### 1.3 与算法读数逐项对照

| | 焊环圆心 | 焊环半径 | 孔位圆心 | 孔位半径 | 孔/环 | 偏移量 |
|---|---|---|---|---|---|---|
| Img000 算法 | (2569.0,1730.4) | 482.8 | **(2641.9,1712.4)** | **170.3** | **0.353** | **1.761mm** |
| Img000 真值 | (2569.0,1730.4)¹ | 482.8¹ | (2389.4,1791.7) | 143.3 | 0.297 | **4.45mm** |
| 误差 | — | — | **264.7px = 6.21mm** | 1.19×（若以卡尺实测的 5mm 孔半径 106.6px 为基准则为 1.60×） | — | **偏低 2.5×** |
| Img009 算法 | (2656,1934) | 448.0 | (2648.6,1913.8) | 195.1 | 0.435→异常 | 无（异常） |
| Img009 真值 | (2656,1934)¹ | 448.0¹ | (2739.9,1917.9) | 113.6 | 0.254 | **2.00mm** |
| 误差 | — | — | 91.4px = 2.14mm | **1.72×** | — | 报异常而非给读数 |

¹ 焊环一列取算法输出（用户对红圈已认可）；孔位真值来自用户蓝圈，与算法无关。

> **Img000 真值偏移的区间与旁证**：真值取决于用哪个焊环圆心。用算法自己报的 `(2569,1730)` 得 **4.45mm**；
> 用 FIXPLAN §4.1 从早前截图反解出的用户手绘焊环 `(2632,1763)`（比算法偏 34px）得 **5.74mm**。
> 两条旁证落在同一个区间：① **相隔 1 秒**的下一帧 `19-22-07`（分层路径，孔位经凹陷圆环基准验证为 0.25mm 准确）
> 报 **4.92mm**；② 凹陷圆环基准给出的该帧真值 **4.13mm**。
> 因此真值 ≈ **4.1~5.7mm**，本报表取 4.45mm 为例；无论如何，算法报的 **1.761mm 偏低 2.4~3.3 倍**。

**两种失效的后果完全不同，必须以不同优先级对待：**

- Img000 型 = **静默错误读数**（偏低 2.5 倍，还报 OK）。它会污染工艺趋势、SPC 与焊偏判定，
  属于"该判 NG 却判合格"的方向。
- Img009 型 = **假异常**（真值 2.00mm 本来可测）。方向是安全的（交人工复核），
  但每 933 帧里会凭空产生 4 帧无谓复核。

---

## 2. 复现与取证方法（可重跑）

| 手段 | 做了什么 | 关键产出 |
|---|---|---|
| `WeldRingLocator.Detect` / `HoleLocator.Locate` / `PinLocator.Locate` 反射探针 | 不修改产品代码，直接调用内部类，**复刻算法的焊环选择逻辑**（主路径 → 核宽阶梯 → 双尺度），拿到"算法真正传给 HoleLocator 的那个环" | 逐帧判定孔位来自哪条路径；`pin_paths.csv`（933 帧全量几何） |
| 三层分层的复算 | 按 `HoleLocator` 的实现逐步复算搜索区、递归 Otsu、中间层掩码与连通域 | 证明"分层为什么返回 null"（中间层 4.7%/3.6%、开运算后 0 个 ≥60px 连通域） |
| 用户标注反解（`userblue2.py`） | 颜色分离 + 用算法自画的圈反解映射 | 两帧的真值孔位（§1.2） |
| **凹陷圆环基准**（`groove.py`） | Hough 找用户卡尺量过的 30mm 凹陷圆环（与孔同心），作为**与成像无关的机械基准** | 4 帧上 `(凹陷心 − 孔心) = 0.2~0.4mm`，其中含 Img000 那帧（用用户蓝圈算得 0.35mm）→ 反证孔位真值可信 |
| 帧间纹理一致性（`texture_id.py`） | 用抗光照的局部能量图（而非灰度）做互相关 | 相隔 1 秒的两帧相关峰值 **0.800**，跨零件基线 **0.40**，相隔数小时降到 0.34~0.49 → 1 秒对确实是同一场景 |
| 候选修法原型（`prototype.py`） | 在同样搜索区、同样环输入下比较 5 种分层取法 | 见 §5 S1 |

---

## 3. 根因链（含代码位置）

### RC-1 分层判据在"端面金属被冲白"时整体塌掉 → `HoleLocator` 返回 null

`HoleLocator.Locate` 用**递归 Otsu**（`RecursiveOtsu`，先暗/亮二分、再在亮侧二分）分三层：
暗缝隙 / 孔位端面（灰盘）/ 端面金属（最亮）。取中间层 (t1, t2]，开运算后取面积最大的连通域。

它的**隐含前提**是"孔位端面是中间灰度层"。但在本工况下：

| 帧 | 搜索区（0.45×环半径） | t1 | t2 | 中间层占比 | 开运算后面积≥60px 的连通域 |
|---|---|---|---|---|---|
| Img000 | ≈2.8×10³px | 198 | 234 | **133px = 4.7%** | **0 个 → 放弃** |
| Img009（C# 直接调用内部类实测） | 2835px | 199 | 237 | **103px = 3.6%** | **0 个 → 放弃** |

机理：**端面金属被反光冲白到 250+，占了搜索区约一半（Img009 实测 47.2%）**，于是第一次 Otsu 的阈值 t1 落在
"孔位端面（约 180~210）"与"冲白金属（250+）"之间 —— 灰盘被划进**暗层**，而"中间层"只剩下
灰盘与冲白区之间的**抗锯齿过渡带**（几个像素宽的细环）。3×3 开运算把它彻底抹掉，
连通域一个都不剩，`best == null`，`Locate` 返回 null。

> 注意：`HoleMinClusterSeparation`（门槛 12）实测为 35/38，**放行了**；真正把定位打死的是
> "中间层被切没了" + "≥60px 连通域"这两个后置条件。
> 另外文件里的 `KMeans3`（三均值聚类）是**死代码**——写了但没被调用，说明这条路线当初被搁置了。

**这就是"焊环与孔洞几乎连在一起"极端工况的机制**：冲白区（端面金属环带）在孔沿与熔核内沿之间
被挤薄甚至挤没，三类材质退化成两类，分层判据失效。

### RC-2 兜底路径无条件接手，且它跑在自己的可测域之外

`WeldInspectionAlgorithm.cs:252-256`：`HoleLocator` 返回 null 时**无条件**调用
`PinLocator.Locate(work, ring.Center, ring.BaseSize, _cfg)`。这条路径的模型是
"射线原点处是一块光滑圆盘，沿射线第一次显著上升沿即盘沿"，而**射线原点是焊环圆心**。

- 搜索窗 = `[0.09, 1.55] × PinRadiusFamilyRatio×base = [2.0, 33.5]` 工作像素。
- Img000 的真孔心距焊环圆心 **23.9 工作像素**（= 1.32 倍真孔半径，真孔半径 18.1 工作像素），
  孔的最远沿在 **42 工作像素**处 —— **整个远端落在窗口之外**。
- 于是朝孔一侧的射线抓到孔沿（约 16px），背孔一侧的射线抓到熔核内沿（约 24px），
  正弦拟合把两者平均成一个 `r=21.5px、圆心离焊环圆心 9.5px` 的圆——**既不是孔，也不是熔核内沿**。
- 该拟合自报的 `D/q = 0.44`，**落在"可靠域 ≤0.8"之内**，所以它没有任何机制察觉自己错了。
  （配置注释已写明"卡尺模型只在 D < 0.8×孔半径 时可靠"；本帧真值 `D/q_true ≈ 1.32`。）

### RC-3 唯一的体检闸门拦不住它，而且口径不自洽

孔位唯一的合理性校验是 `pinRatio = pin.Radius / ring.Radius` 是否落在 `[0.10, 0.40]`：

- 卡尺兜底帧的孔/环比**中位 0.306**、p95 0.367、max 0.400 —— **68/72 帧静默通过**（Img000 即 0.353）。
- 与分层帧（中位 0.226）的分布只有**中位数**分得开，单帧重叠（分层 max 0.298 > 卡尺 p5 0.167）。
- Img009 之所以报异常，是因为**分子分母口径不同**：孔位在**精修前**的环（r=66.78 工作像素）上测量，
  而闸门用**精修后**的环（r=56.5，−15%）做分母 → 0.368 变成 0.435 才越界。
  同一个错误测量，**仅仅因为精修把环搬动了多少**，就在"静默通过"和"报异常"之间摇摆。
- 没有任何检查针对**孔位自身的族值**（`PinRadiusFamilyRatio` 存在但只当尺度基准，不当判据），
  也没有检查卡尺的角向一致性（Img000 的卡尺 173/180 条射线命中，命中率好看，
  但"半圈孔沿 + 半圈熔核内沿"拼出来的圆在角向残差上会暴露）。

### RC-4 两个地标的可靠性没有区分（**本条结论已按用户澄清修正**）

偏移量 = 焊环心 − 孔心，两侧被当作同等可信。

- 当时（假设 24 帧为同一零件重拍）我用它推出"焊环跟着零件走、孔位在乱跳"：相隔 1 秒的
  `19-22-06` 与 `19-22-07` 上，凹陷圆环心移动 48px、焊环心移动 59px（相对凹陷圆环只变 12px），
  而"孔心"移动 264px。
- **用户 2026-09-18 确认：一个 SN 目录 = 一条线上的 24 个零件**。于是"相隔 1 秒的两帧"
  是**两个不同零件**，上面那组位移里既含零件差异也含测量误差，**不能用来做归因**。
  同理，"纹理一致性 0.80（对照跨零件基线 0.40）"只能说明**相邻零件的焊缝纹理相似**
  （同一焊接程序），不能证明是同一零件。
- **因此本报表不再依赖任何跨帧/刚体推断**，误差判定全部改用绝对真值：
  用户手绘标注反解（§1.2）+ 凹陷圆环机械基准（§2）。这两条对**单帧**成立，不需要零件刚性假设。
- 结论仍然成立的版本：**卡尺兜底这条路径在单帧上就会被证明是错的**——Img000 那一帧，
  卡尺给的孔心离凹陷圆环心 6.4mm（离用户真值 6.21mm），而真孔位与凹陷圆环只差 0.35mm。
  这与"是否同一零件"无关。

### RC-5 回归测试抓不住这条路

- `PpSampleTests` 断言的是**中位数**：`median(pin/ring) ∈ [0.17,0.32]`、`median(D) ∈ [0.9,3.2]mm`。
  7.7% 的错误帧动不了中位数，所以**整条错误路径在 CI 里是隐形的**（实测 median pin/ring 0.226 稳稳在带内）。
- `PpLocatorAgreementTests` 只对**焊环**做厂商一致性校验，孔位没有绝对基准。
- `RingCredibilityGateTests` / `WeldRingBoundaryTests` 同样只钉焊环。
- 结果：本次用户报的两帧，在既有回归套件里没有一条断言能抓到（套件全部围绕焊环半径/支撑弧段/偏移量中位数）。

### 附：一个连带影响

孔位错了会连带打死**熔核内沿**（环宽）：`WeldInnerLocator` 的搜索起点是 `1.15 × 极柱半径`，
以焊环圆心为原点。Img000 的起点被算成 `1.15×170.3 = 196px`（落在熔核内部），于是"持续超阈"判据永远不满足，
界面显示"本次未取到熔核内沿"——与两张截图一致。

---

## 4. 影响面（40 SN / 933 可测帧，全量批跑）

| 孔位来源 | 帧数 | 占比 | 孔/环比 p5 / 中位 / p95 |
|---|---|---|---|
| 分层（含外径弧） | 861 | 92.3% | 0.218 / **0.226** / 0.249 |
| **卡尺兜底** | **72** | **7.7%** | 0.167 / **0.306** / 0.367 |
| 焊环未定位（另计） | 27 / 960 | 2.8% | — |
| 端到端"异常" | 4 | 0.4% | **100% 来自卡尺兜底** |

- 卡尺兜底帧相对**本 SN 分层帧中位偏移**的偏离：中位 **2.15mm**、p90 3.04mm、最大 4.85mm。
- 同一 SN 内偏移量读数跨度：最小 0.09~3.81mm，典型 0.3~4.9mm，最大 0.15~5.4mm。
- 4 帧"异常"分布在 4 个不同 SN 上，说明这不是个别零件的偶发问题。

**结论：约每 13 帧就有 1 帧的孔位来自一条已被证明会量错的路径**，其中大部分静默通过，
少数以"孔洞半径异常"报出。

---

## 5. 解决方案

> 排序原则：先做**代价小、方向安全**的（S3/S4），再做**能真正修好**的（S1/S2），
> 长期补**真值基建**（S5）与**逐帧断言**（S6）。
> 所有数字为原型/批跑实测；标注「未验证」的表示尚未在实际代码里落地。

### S1【核心·已原型验证】把"孔位端面"的分层判据换掉

- **改动**：`HoleLocator` 的中间层取法由「递归 Otsu」改为「**三类 Otsu（两阈值联合优化）**」
  （或启用同文件里已有的 `KMeans3`）；中间层掩码**去掉 3×3 开运算**，改用
  `area ≥ max(60, 0.002×base²)` + 形状/位置择优（圆度从"硬否决"降为"候选排序依据"）。
- **原型实测**（同搜索区、同输入环，两帧）：

| 方案 | Img000 孔心误差 | Img009 孔心误差 |
|---|---|---|
| V1 现行（递归 Otsu + 开运算 + area≥60） | **无候选**（复现了 null） | **无候选** |
| V3 三类 Otsu + 不开运算 + area≥60 | **23px = 0.55mm** | **59px = 1.38mm** |
| V2 三类 Otsu + 闭运算 + area≥0.002base² | 205px = 4.82mm（被熔核中灰纹理污染） | 15px = 0.36mm |

- **注意**：V3 的**半径不稳**（Img000 0.144×环 vs Img009 0.202×环），因为中间层掩码只覆盖灰盘
  被照亮的那一部分。**所以 S1 必须与 S2 配套**：S1 负责圆心，S2 负责半径。
- **风险**：92.3% 的良品帧走同一条路径，改判据有回归风险 → 必须用本轮的 40 SN/933 帧批跑工具
  对比改动前后的 `pin/ring` 分布、卡尺兜底率、端到端异常率。

### S2【与 S1 配套】半径交给"外径弧"，并复核它的闸门

- `HoleLocator.FitOuterArc` 已经是正确机理（拟合"孔位端面 ↔ 被冲白的端面金属"之间的亮度台阶弧），
  且它的搜索范围本来就按 `MaxHoleRadiusRatio×环半径` 给出、不依赖种子半径。
- **需要复核的一处**：`MaxPinArcRadiusRatioOfRing = 0.30` 与 Img000 的真值 0.297 **只差 1%**，
  会把"种子圆心中了、半径该被补全"的情况又挡回去。建议按新口径重标到 **0.33**，
  并补上依据（当前 0.30 是按"外径弧走偏"的 Img003 一帧定的）。
- `FitOuterArc` 要求 `arcRadius ≥ 1.15×种子半径`：S1 给出的种子偏小（0.144×环），
  真值 0.25~0.30×环 → 1.7~2.1 倍，能过；但这条耦合关系要在测试里钉住。

### S3【安全网·可独立先行】兜底路径必须自带有效性判据

在 `WeldInspectionAlgorithm` 调用 `PinLocator.Locate` 之后加校验，任一不满足即
**报"测量失败：孔位不可信"交人工复核**（而不是给一个静默错误的偏移量）。

先把"哪些量分不开"讲清楚，避免又走一次弯路：

| 候选判据 | 实测是否可分 | 说明 |
|---|---|---|
| 孔位**绝对半径** vs 自身族值（`PinRadiusFamilyRatio×base` = 21.6 工作像素） | **分不开** | 卡尺的搜索窗本来就锚在族值上，它的解会饱和在族值附近 0.94×族值；真值 Img000 0.84×、Img009 0.66×，分层帧 0.70×——重叠 |
| 孔/环比 `[0.10,0.40]` | **分不开**（68/72 通过） | 卡尺 0.306 vs 分层 0.226 只是**统计**差异，单帧重叠（分层 max 0.298 > 卡尺 p5 0.167） |

真正有区分力的是**结构判据**——"这个圆的边界是用什么拼出来的"：

1. **角向一致性**：卡尺给出的逐射线边界半径做一阶谐波拟合后，残差的角向标准差
   ≤ `0.06 × 半径`。Img000 的圆是"半圈孔沿（~16px）+ 半圈熔核内沿（~24px）"拼的，
   角向系统差会把 RMS 顶上去；真孔沿的粗糙度不会有这种整半圈的台阶。
2. **圆内材质一致性**：沿 360° 采样拟合圆**内侧**与**紧外侧**的灰度/纹理，
   真孔的边界几乎处处是"内侧中灰孔面 / 外侧暗缝隙"；卡尺那种混合圆的边界有一半是
   "内侧冲白金属 / 外侧粗糙熔核"。要求"满足孔沿特征的方向占比 ≥0.6"。
3. **与分层路径交叉验证**：分层路径只要给出过候选（哪怕形状不合格），两者圆心差
   > `0.5 × 孔半径` 即判不可信。（Img000：分层无候选，此条不触发；Img009：同样无候选。
   但这类"两条路径都给出结果却互相矛盾"的帧在其它工况下会触发。）
4. **弱过滤（只作投票，不作单独门限）**：孔位半径 / 族值 ∈ `[0.55, 0.90]`。
   实测真值落在 0.66~0.84、分层帧中位 0.70，而卡尺帧中位 0.94——这条能挡掉大部分卡尺帧，
   但因为分布有重叠，必须与 1/2/3 联合使用。

> 这条**即使 S1/S2 不动也建议先做**：它把"错误读数"变成"复核"，
> 与仓库既有的取舍（宁可不测，不可伪造）一致。

### S4【口径修正·低风险】让闸门的分子分母同源

`pinRatio` 在精修**之后**才校验，而孔位在精修**之前**测量（Img009 因此 0.368→0.435 越界）。
改法二选一：① 把孔位校验也放在精修之前；② 精修后重算 `pinRatio` 时显式记录"测量时所用环半径"，
并把该环半径写进结果（便于报表/界面解释）。不改变任何阈值，只让判定可解释、可复现。

### S5【真值基建·长期】把"凹陷圆环（30mm）"做成逐帧可用的第三方基准

用户卡尺实测「孔 5mm、凹陷圆环 30mm、二者同心」。**这是本产品唯一与成像无关的机械基准**：

- 厂商 `.vdb` 的圆、独立纹理边界 RANSAC、本项目的 `|∇I|` 自锚精修 —— **三者都是梯度/纹理类估计器**，
  同一片冲白会把它们同步带偏（FIXPLAN §4.2 用"三者互相一致"来证明焊环没偏，这个论证是循环的：
  它们共享同一个失效模式）。
- 凹陷圆环是**机械加工特征**，不受光照影响，可以给孔位一个绝对参照（孔心应等于凹陷圆心，
  实测 0.12~0.35mm）。
- 本轮 Hough 只成功 4/24 帧（圆环细而暗），需要专门做：以孔位粗定位为原点做径向 `|∇I|` 中位剖面
  + 亚像素圆弧拟合。

**顺带记录一个尚未定论的观察（需要 S5 才能判定）**：那 4 帧的 `(凹陷环心 − 焊环心)` 分别是
`-4.48 / -4.74 / -0.08 / -0.39 mm`。若 24 帧确为同一刚性零件，这意味着**焊环侧也在漂 ~4.4mm**；
但这 4 帧的纹理一致性只有 0.34（低于跨零件基线 0.40），**可能是不同零件**，故暂不下结论。
需要用户确认一件事实：**一个 SN 目录 = 一个零件被反复拍摄 24 次，还是一条线上的 24 个零件？**
（两种解释下"跨帧 7.4mm 散度"的性质完全不同：前者是测量误差，后者含真实零件间差异。）

### S6【回归测试】用绝对真值钉住，别再只断言中位数

- 新增 `HoleTruthTests`（用用户两张标注反解出的真值，逐帧断言）：
  - Img000：孔心 ∈ `(2389,1792) ± 60px`，半径 ∈ `[0.24, 0.32] × 焊环半径`，偏移量 ∈ `[3.8, 5.2]mm`；
  - Img009：孔心 ∈ `(2740,1918) ± 70px`，半径 ∈ `[0.20, 0.28] × 焊环半径`，**不得报"孔洞半径异常"**。
- 新增批跑守卫（用本轮已在 `.scratch/CaseProbe` 的探针）：
  ① 卡尺兜底帧占比 ≤1%；② 卡尺帧与分层帧的孔/环比中位之差 ≤0.03；
  ③ 端到端"异常"帧必须**全部**能由焊环可信度解释（不允许出现"孔洞半径异常"）；
  ④ 逐帧断言"孔/环比 > 0.28 的帧必须给出不可信理由"。
- **不要**再用"跨帧一致性/稳定性"当精度指标（FIXPLAN §4.6 的教训）。本轮它同样会误导：
  7.4mm 的跨帧散度在"同一零件"和"不同零件"两种解释下含义相反。
  精度指标必须配一条与**外部绝对基准**（S5）对齐的量。

---

## 6. 优先级与验收

| 阶段 | 内容 | 预期收益（实测依据） | 验收 |
|---|---|---|---|
| P0（半天） | S3 兜底有效性判据 + S4 口径统一 | 把 7.7% 中"错误读数"的那部分转为"复核"，消除静默错误偏移 | 4 帧端到端异常不再来自"孔洞半径异常"；Img000 报"孔位不可信"而不是 1.761mm |
| P1（1~2 天） | S1 分层判据 + S2 外径弧口径 | 两帧孔心误差 6.21/2.14mm → **0.55/1.38mm**（原型实测） | 40 SN/933 帧批跑：卡尺兜底率 ≤1%、良品帧 pin/ring 中位不劣化；`HoleTruthTests` 通过 |
| P2（2~3 天） | S5 凹陷圆环基准 | 首次给出与成像无关的绝对基准 | 逐帧可得率 ≥80%，孔心与凹陷圆心差 ≤0.4mm 的帧占比 ≥90% |
| P3 | S6 逐帧断言 + 批跑守卫接入 CI | 防止本类错误再次隐形通过 | `dotnet test` 全绿且新增守卫全部生效 |

---

## 7. 已排除 / 不建议的方向

| 方向 | 为什么不做 |
|---|---|
| 只放宽/收紧 `MaxPinRadiusRatio` | 分布重叠（分层 max 0.298 > 卡尺 p5 0.167），调它只会把错误从"静默"挪到"假异常"或反过来（Img009 就是被它误杀的真值 2.00mm 帧） |
| 继续加"跨帧一致性"类指标 | 对"把测量压成常数"没有鉴别力，FIXPLAN §4.6 已记录一次被它奖励的错误修法 |
| 用灰度阈值直接找孔 | 端面金属被冲白、孔位端面是中灰，两者随光照互换，任何绝对灰度判据都会失效 |
| 把孔位搜索区调大/调小 | 本轮实测：调小（0.30×环）让三类分层更差（中间层 1.3%/0.7%，仍无候选），不是解 |
| 改判定公差来"消化"这个问题 | 公差是工艺决策（`OffsetToleranceMm` 已注明是筛查限）；本问题改的是**测量**，不是判据 |

---

## 8. 指定 SN 的端到端结果（`SN_CCSAGZZG01A2536925090900003025080700034`，24 帧）

用当前源码树跑 `WeldInspectionAlgorithm.Inspect` + `Annotate`，逐帧结果与标注图：

- 标注图（24 张，含用户手绘真值蓝圈的**蓝圈叠加**）：`.scratch/case000/sn0003_annotated/*.png`
- 复现命令：`CaseProbe.exe sn SN_CCSAGZZG01A2536925090900003025080700034 <输出目录>`

| 帧 | 路径 | 孔/环 | 偏移mm | 孔心(原图) | 孔半径 | 判定 |
|---|---|---|---|---|---|---|
| 16-48-48 | 分层 | 0.274 | 2.111 | (2539,1801) | 131 | OK |
| 16-48-50 | 分层 | 0.262 | 2.911 | (2575,1817) | 130 | OK |
| 16-48-51 | 分层 | 0.244 | 2.032 | (2610,1804) | 119 | OK |
| 16-48-53 | 分层 | 0.248 | 1.991 | (2597,1821) | 120 | OK |
| 16-48-55 | 分层 | 0.245 | 1.058 | (2647,1855) | 119 | OK |
| **16-48-56** | **卡尺兜底** | **0.309** | 1.509 | (2533,1881) | **149** | OK（孔/环比已偏高，静默通过） |
| 16-48-58 | 分层 | 0.252 | 1.155 | (2655,1874) | 122 | OK |
| 16-48-59 | 分层 | 0.247 | 0.764 | (2708,1895) | 120 | OK |
| 16-49-01 | 分层 | 0.243 | 1.880 | (2679,1926) | 118 | OK |
| **16-49-03（= Img009）** | **卡尺兜底** | **0.435** | — | **(2649,1914)** | **195** | **异常：孔洞半径异常** |
| 16-49-04 | 分层 | 0.286 | 3.305 | (2761,1949) | 141 | OK |
| 16-49-06 | 分层 | 0.305 | 2.977 | (2745,1943) | 147 | OK |
| 16-49-07 ~ 16-49-25（13 帧） | 分层 | 0.224~0.249 | 1.196~2.681 | — | 118~121 | 全部 OK |

- 孔位来源：**分层 22 帧 / 卡尺兜底 2 帧**（8.3%，与本 SN 的全局 7.7% 一致）。
- **16-49-03 的孔位真值（用户蓝圈反解）`(2740,1918) r=114`**：算法给的 `(2649,1914) r=195`
  —— 圆心偏 **2.14mm**、半径 **1.71 倍**，于是被判"孔洞半径异常"。
  相邻的 22 帧分层结果半径都在 **118~147**（中位 120），**与真值 114 吻合**，
  这再次印证 §3 的结论：分层路径是对的，卡尺兜底是错的。
- **16-48-56 是同 SN 内另一个静默通过的错误帧**：它的孔/环比 0.309 明显高于同 SN 分层帧的 0.224~0.249，
  但因为落在 `[0.10, 0.40]` 的闸门内而报 OK，偏移量 1.509mm 不可信。这正是 S3 要拦的形态。

---

## 9. 附件（脚本与数据，均可重跑）

| 路径 | 用途 |
|---|---|
| `.scratch/CaseProbe/` | 反射探针：① `CaseProbe [N]` 批跑 N 个 SN 判定孔位来源，导出 `bin/Debug/net8.0/pin_paths.csv`；② `CaseProbe sn <SN后缀> [输出目录]` 单 SN 端到端 + 标注图（`SnRunner.cs`），含"同帧跑两遍自检确定性" |
| `.scratch/case000/sn0003_annotated/*.png` | §8 那个 SN 的 24 张标注图（Img009 那张叠了用户真值蓝圈） |
| `.scratch/case000/sn0002_annotated/19-22-06.png` | **Img000 的标注图 + 用户真值蓝圈**：界面写 `OK D=1.758mm`，蓝圈（真值孔位）与绿圈（算法孔位）分离，真偏移≈4.46mm——本报表最直观的一张图 |
| `.scratch/case000/sn0003_annotated/16-49-03.png` | Img009 同款：蓝圈正套在灰盘上，算法报"孔洞半径异常" |
| `.scratch/case000/userblue2.py` | 从用户截图反解手绘圈到原图坐标（真值） |
| `.scratch/case000/prototype.py` | S1 的 5 种分层取法原型对比 |
| `.scratch/case000/groove.py` | 凹陷圆环机械基准（Hough）+ 与焊环/孔位的关系 |
| `.scratch/case000/similarity.py` / `texture_id.py` | 帧间刚体变换与抗光照纹理一致性（判定是否同一零件/场景） |
| `.scratch/case000/analyze2.py` | 40 SN 跨帧稳定性统计（偏移向量为刚性不变量） |
| `.scratch/case000/Img000_grid.png` / `Img009_grid.png` / `sn0002_montage.png` | 带坐标网格的放大核对图与逐帧叠加图 |

### 复现命令

```bat
cd .scratch\CaseProbe && dotnet build
bin\Debug\net8.0\CaseProbe.exe 40                                   :: 批跑 40 SN（约 6 分钟）-> pin_paths.csv
bin\Debug\net8.0\CaseProbe.exe sn SN_CCSAGZZG01A2536925090900003025080700034 out_sn  :: 单 SN 端到端 + 标注图
cd ..\case000
python userblue2.py    :: 用户标注真值
python prototype.py    :: S1 原型对比
python analyze2.py     :: 跨帧稳定性
```

---

## 10. 实施结果与验证（2026-09-18 落地）

> 本节记录**真正改到产品代码里**的部分与实测结果；§5 是方案设计，本节是实施后的数字。
> FIXPLAN 的执行记录见 [`FIXPLAN.md`](FIXPLAN.md) 第 5 节。

### 10.1 改了什么

| 文件 | 改动 |
|---|---|
| `Vision/HoleLocator.cs` | ① 分层由"只用递归 Otsu"改为**两种分法各出候选**：递归 Otsu（旧）→ **三类 Otsu**（新增）；**带内仍是"面积从大到小"、带间按此顺序拼接** → 不冲白的帧拿到的第一个候选与改动前逐像素相同；② 外径弧半径增长门限 **1.15×种子 → 1.05×种子**；③ 新增"**弧必须精化种子、不得另找一个圆**"判据（拟合圆心离种子圆心 ≤ `max(1.0×种子半径, 6px)`）；④ 新增 `ThreeClassOtsu`（256 档前缀和穷举两阈值，确定性、无绝对灰度阈值） |
| `Vision/WeldInspectionAlgorithm.cs` | 卡尺兜底加**有效性判据**（残差/半径、半径/族值半径），越界报"孔位不可信"交人工复核；「孔洞半径异常」报文补上**测量时**与**判定时**两个焊环半径 |
| `Models/InspectionConfig.cs` | 新增 `CaliperMaxResidualRatio = 0.10`、`CaliperMaxRadiusRatioOfFamily = 1.10`（含实测分布与口径说明） |
| `Configuration/AppConfig.cs` | `CurrentConfigVersion` **2 → 3** |
| `tests/.../HoleTruthTests.cs` | **新增 3 个用例**（用用户标注反解的真值逐帧断言，见 10.4） |

**S1/S2/S3/S4 的落地状态**：S1（分层判据）✅ 落地且验证；S2（外径弧口径）✅ 以"放宽增长门限 +
新增种子一致性判据"的形式落地（`MaxPinArcRadiusRatioOfRing` 保持 0.30 未改——实测 Img000 的
真值比 0.271 在门限内，原先担心的 0.297 是按精修后环半径算的，而外径弧用的是测量时的环半径）；
S3（兜底有效性判据）✅ 落地；S4（口径统一）✅ 以"报文带两个环半径"的形式落地（保留用精修后环做
分母，因为那是更准的环）；S5（凹陷圆环基准）/S6（逐帧断言）——S6 已落地（`HoleTruthTests`），
S5 仍是待办。

### 10.2 零回归验证（关键）

同一个 **20 SN / 468 帧**子集，逐帧对比改动前后的孔位半径与偏移量：

| 项 | 结果 |
|---|---|
| 原本走分层、现在仍分层 | **435 帧，逐帧完全一致（半径与偏移量都相同）** |
| 路径发生变化的帧 | **32 帧，全部是"卡尺兜底 → 分层"**（没有反向变化） |
| 端到端判定 | `{OK 465, ERR 3}` → **`{OK 467, ERR 1}`**（无新增 NG） |

全量 **40 SN / 933 可测帧**：

| | 改动前 | 改动后 |
|---|---|---|
| 分层路径 | 861 帧（92.3%） | **913 帧（97.9%）** |
| 卡尺兜底 | 72 帧（7.7%） | **20 帧（2.1%）** |
| 端到端异常 | 4 帧 | 17 帧（其中 16 帧是把**可疑读数**改为人工复核——安全方向；1 帧是原先的假异常已修复） |

### 10.3 用户报的两帧：改动前后与真值对照

| 帧 | 改动前 | 改动后 | 真值（用户标注反解） |
|---|---|---|---|
| **Img000**（19-22-06） | 卡尺兜底，孔心 (2642,1712) r=170 → **偏移量 1.761mm、判 OK** | 分层，孔心 **(2379,1775) r=141** → **偏移量 4.587mm** | (2389,1792) r=143；偏移量 ≈4.45mm（区间 4.1~5.7） |
| | 孔心误差 **6.21mm** | 孔心误差 **0.46mm** | — |
| **Img009**（16-49-03） | 卡尺兜底，孔心 (2649,1914) r=195 → **报"孔洞半径异常"** | 分层，孔心 **(2733,1976) r=126** → **D=2.069mm** | (2740,1918) r=114；D=2.00mm |
| | 半径 **1.72×**、无读数 | 半径 1.11×、D 误差 0.07mm | — |

### 10.4 新增回归测试（绝对真值，不用中位数）

`tests/BatteryWeldAOI.Core.Tests/HoleTruthTests.cs`，3 个用例全部通过（2.8s）：

1. **两帧都必须测得出**，孔心离真值 ≤60px、半径比 0.85~1.25（实测 19.7px / 0.98 与 58px / 1.11）；
2. **Img000 的偏移量必须落在 3.4~5.5mm**——不许再被压低（改动前 1.761mm）；
3. **孔位半径必须与卡尺实测的 5mm 孔吻合**（比值 0.75~1.35）——这条是机理守卫，防止"结果被别的
   口径巧合修好"（卡尺在这两帧给的是 1.60× / 1.83×，会被拦下）。

为什么用逐帧绝对真值而不是既有 pp 用例的"中位数落在带内"：7.7% 的错误帧动不了中位数
（实测 median 孔/环比始终 0.226，稳稳在带内），那种口径对**路径走错**没有鉴别力。

### 10.5 实施过程中发现并修掉的一个**新**失败模式（务必记住）

三类 Otsu 带里，熔核内部的**中灰碎片**同样可能是"面积最大 + 圆度够 + 半径比正常"的块。
实测 `SN_...0033` 的 `15-52-39`：真孔位（面积最大的候选）的外径弧给 r=15.29，
**只差 1.4% 没够到 `1.15×种子` 的老门限**（要求 15.5）而被判无效；循环于是走到下一块碎片
（种子半径仅 4.6px），弧在那块碎片上凑出一个 r=15.29、圆心离真值 0.56×环半径的圆并通过全部闸门
→ 报出 **6.90mm 的假 NG**（该帧真孔位其实与焊环同心，D≈0.08mm）。
两条修法（放宽到 1.05 + 新增"弧必须精化种子"判据）落地后该帧恢复为 **D=0.082mm、判定 OK**。

**教训**：把"候选集合"扩大以后，**"候选之间如何竞争"的规则必须一并审查**——原来的"面积最大"
在新集合上不再等价于"最像孔位"，而外径弧的门限（1.15×）原本是按"种子只有孔位的一小部分"
标定的，遇到"种子已接近完整孔位"的情形就会误判。

### 10.6 被否定的候选判据（据实记录，不要重走）

| 候选 | 实测结论 |
|---|---|
| 候选打分 = 圆度 × 位置合理度（取代"面积最大"） | **失败**：SN_...0002 上把 19-22-11/12/19 三帧从"分层正确"打成"卡尺→异常"。面积最大的候选才是孔位端面；高圆度的碎斑反而更小。已回退 |
| 冲白剔除（连通域中位/p75 灰度 ≥ 图 p99.9−2 即剔除） | **不必要**：三类 Otsu 的中间层上界（实测 215）本就低于冲白平台（250+），带本身已排除它；加上它只是多一条要标定的绝对量 |
| 「候选圆外侧环带的纹理能量低」＝孔位被光滑材质包围 | **失败且方向相反**：真孔位 0.43、对照的熔核碎片 0.80（碎片周围也常有暗缝隙） |
| 形态学改闭运算以保住被切薄的中间层 | **不需要**：三类 Otsu 带里 3×3 开运算就够（Img000 候选 area 306 vs 不开运算 308，结果差 0.02mm） |
| 用更大的面积门限排除熔核碎片 | 未采用：碎片的面积可以比真孔位更大，分不开 |

### 10.7 残留（据实记录）

1. **仍走卡尺兜底的 20 帧**（2.1%）中 17 帧被新判据拦下改为人工复核。这些帧的孔位端面在灰度上
   确实与周围分不开（三类 Otsu 也给不出候选），要救需要新机理（"暗缝隙包围的孤岛"+ 弧验证的多
   假设搜索），本轮未做。
2. **`CaliperMaxResidualRatio = 0.10` 是口径选择，不是分界**：卡尺帧的残差/半径实测中位就是
   0.166——该路径在本产品上整体不可靠（真值 D/q ≈ 1.3，远超其模型上限 0.8）。放宽到 0.22（p75）
   可让约一半的帧重新给出读数，代价是**接受可疑读数**，需用户/工艺决策。
3. **"孔位端面 = 中间灰度层"这一层语义未改**：本轮只是增加了第二种分法。
4. **判定公差 6.6mm 未动**（扫描限，非工艺规格）；本次修复后 Img000 的读数 4.587mm 落在此限内，
   若工艺公差比它小得多，该帧本就该判 NG —— 那是**正确行为**，不是新问题。

---

## 11. 回归验证的更正（含一次自己踩的坑）

> 本节是对 §10.2 / §10.7 的**更正与补充**，因为文档原先只写了"全量测试 141/143"，
> 而实际树上的既有失败远多于此。

### 11.1 既有失败的基线（用"回滚 HoleLocator"实测，不是照抄文档）

把本轮的 HoleLocator 改动**临时回滚**后重跑同一批用例：

| 口径 | 改动前（回滚） | 改动后 |
|---|---|---|
| `WeldInspectionAlgorithmTests` + `RealisticRobustnessTests`（合成夹具） | **29 例失败** | **10 例失败** |
| `ImagePipelineTests.FaultyImage…` | 1 例（用"禁用分带"的探针复现，输出逐字相同 → 既有） | 1 例（同） |
| `HoleTruthTests`（本轮新增） | 3 例失败（改动被回滚，本就该失败） | **3/3 通过** |
| tests/pp 与旧相机语料：`PpSampleTests` / `NgSampleTests` / `RealSampleTests` / `WeldRingBoundaryTests` / `RingWidthTests` / `OffsetScaleTests` | — | **全部通过** |

**净效果：修好 18 例、新增 0 例**——改动后的 11 例失败逐条都能在改动前的 29 例里找到
（失败清单逐条对比见 `.scratch/case000/diff2.py` 的输出）。

改动后仍失败的 11 例（**均为既有**，属合成夹具的精度敏感用例，与本次孔位改动无关）：

| 用例 | 现象 |
|---|---|
| `ZeroOffsetPoint_IsOk(seed 2)` | 零偏差点测得 >0.10mm |
| `MisalignedPoint_IsDetected_AndDistanceAccurate(seed 3)` | 真值 0.70mm，实测 0.946mm |
| `Misaligned_NearMeasurableEnvelope_IsMeasuredAccurately(seed 3)` | 真值 0.70mm，实测 0.931mm |
| `DarkWeld_Misaligned_IsDetected_AndDistanceAccurate(seed 2/3/5)` | 实测 1.059 / 2.322 / 0.856mm（偏大） |
| `OkPoint_RandomIllumination_IsOk_AndMeasuresOffset(seed 1/3/4)` | OK 点被误判 Misaligned（D=1.985 / 0.600mm）或 X 偏 −0.319mm |
| `SameScene_DifferentIllumination_GivesConsistentResult` | 光照 A 下 OK 点被误判 Misaligned |
| `ImagePipelineTests.FaultyImage…` | 合成点假 Misaligned（D=6.870mm > 公差 6.6） |

这些用例的共性是"**夹具上的偏移量精度**"，而本轮的改动只影响**孔位从哪里取**——
它们在回滚 HoleLocator 后同样失败，故与本轮无关（**待办**：这批既有失败应由
夹具几何/精修口径那一轮统一处理，见 FIXPLAN §3.3 的残留记录）。

### 11.2 踩到的坑：增量构建会让你对着旧 DLL 下结论

我在做"回滚基线"实验时用 `copy /Y` 恢复源码，而 cmd 的 `copy` **保留源文件时间戳** →
恢复后的文件比上次编译的 DLL 还旧 → **MSBuild 判定无需重编译** → `dotnet test --no-build`
跑的仍是回滚版 DLL，于是真值测试与焊环用例"一起失败"，看起来像是我引入了回归。
改用 `dotnet build --no-incremental` 强制重建后，`HoleTruthTests` 恢复 3/3 通过。

**教训（写给下一个改这里的人）**：
1. 改完源码要跑测试时，用 `dotnet build --no-incremental` 或先删目标 DLL；
2. 用 `copy` 恢复文件后必须确认时间戳比 DLL 新；
3. 探针脚本里的"源码级替换"若静默失败（本次第一次用 Python 内联替换就没生效），
   由它得出的结论完全无效——第二次改用编辑器工具复现才成立。
