# BatteryWeldAOI 开发清单与执行计划

> 电池包焊后 AOI（Automated Optical Inspection）检测系统 —— 开发总控文档
>
> 用途：框架选型说明 + 开发步骤清单 + 执行记录。**每完成一项即在清单打钩，并在「五、执行记录」追加一条记录。**

---

## 一、项目概述

| 项 | 内容 |
|---|---|
| 目标 | 多电池包（动力电池模组/储能包）激光焊后焊点质量检测 |
| 检测项 | 焊偏 Misaligned / 漏焊 MissingWeld / 炸焊 Blowout / 焊渣飞溅 Spatter |
| 工作模式 | Stop-and-Go：PLC 按固定顺序移动 → 到位 → 相机软触发 → 视觉流水线并行处理 |
| 技术栈 | C# / .NET 8 / OpenCvSharp4 / xUnit |
| 项目英文名 | **BatteryWeldAOI**（用于替换原文件夹名「新建文件夹 (2)」） |

## 二、整体框架选型（含备选对比）

| 维度 | 选型 | 备选方案 | 选择理由 |
|---|---|---|---|
| 主语言 | C# / .NET 8 | Python+OpenCV、C++/Qt | 工业上位机绝对主流；与 PLC/相机 SDK 生态契合 |
| 视觉库 | OpenCvSharp4 (MIT) | Halcon、VisionPro | 开源免费、无加密狗；接口风格与 Halcon 同构，后续可平移 |
| 下位机通信 | TCP 文本协议（行分隔） | Modbus TCP / S7 / MC 协议 | 仿真阶段最易调试；已用 `IPlcClient` 接口隔离，真机时替换实现即可 |
| 相机接入 | `ICameraGrabber` 抽象 + 模拟相机 | 海康 MVS / Basler Pylon SDK | 真机接入只需新增一个实现类 |
| 并发模型 | `BlockingCollection` 生产者-消费者（有界队列反压） | `Channel<T>`、无界队列 | 运动线程与视觉线程彻底解耦；有界防内存膨胀 |
| 状态机 | 自研轻量转移表 | Stateless 库 | 零依赖、转移规则集中可测 |
| 坐标标定 | 九点标定（最小二乘仿射，自研） | Halcon 标定板、棋盘格 | 覆盖旋转/缩放/平移，可吸收镜头畸变与安装偏角 |
| UI | 控制台（演示） | WinForms / WPF | 先跑通内核；Phase 2 再加 WPF 界面 |
| 测试 | xUnit | NUnit/MSTest | 模板默认，够用 |

## 三、系统架构

```
┌────────────────────────── 上位机 (BatteryWeldAOI.App) ──────────────────────────┐
│                                                                                │
│  InspectionController（Stop-and-Go 编排）      MachineStateMachine（状态机）   │
│        │ MoveTo → 等到位 → 拍照 → 入队                                          │
│        ▼                          ▲ 结果回传                                    │
│  ICameraGrabber ──► ImagePipeline(BlockingCollection, N workers)                │
│  (SimulatedCamera)        │ Inspect: 霍夫圆找极柱 → 暗域找焊缝 → 仿射换算偏差   │
│        ▲                  ▼                                                    │
│        │ 剧本      NinePointCalibration（九点标定 mm↔px）                       │
│  ImageGenerator（合成图像）                        │                            │
│        │                                          ▼                            │
│        │                                   CsvReportWriter + 标注图输出         │
└────────┼──────────────────────────────────────────┼────────────────────────────┘
         │ TCP (行协议)                              │
         ▼                                          │
┌─ 下位机 ────────────┐                              │
│  PlcSimulator       │◄──── 结果/完成信号 ──────────┘
│  (真实场景=PLC+轴)   │
└─────────────────────┘
```

## 四、代码结构

```
BatteryWeldAOI.sln
├─ src/BatteryWeldAOI.Core/           核心库（无 UI 依赖）
│  ├─ Models/         InspectionConfig, WeldPoint, PointScenario, PointInspectionResult, InspectionReport, MachineState/WeldDefect
│  ├─ StateMachine/   MachineStateMachine（转移表校验，防撞机）
│  ├─ Communication/  PlcProtocol, IPlcClient, TcpPlcClient, PlcSimulator
│  ├─ Camera/         ICameraGrabber, SimulatedCamera
│  ├─ Vision/         NinePointCalibration, ImageGenerator, WeldInspectionAlgorithm
│  ├─ Pipeline/       ImagePipeline（生产者-消费者）
│  ├─ Orchestration/  InspectionController
│  └─ Reporting/      CsvReportWriter
├─ src/BatteryWeldAOI.App/            控制台主程序（接线 + 演示 + 报表）
├─ tests/BatteryWeldAOI.Core.Tests/   xUnit 单元测试
└─ tools/Diag/                        算法诊断工具（开发期定位问题用）
```

## 五、开发步骤清单

> 规则：完成打 `[x]`，进行中标 `[~]`，未开始 `[ ]`；每完成一项在「六、执行记录」追加 R# 记录。

- [x] D1 解决方案骨架：sln + Core/App/Tests 三项目 + NuGet 依赖（OpenCvSharp4 + runtime.win）
- [x] D2 Models + 设备状态机（转移表 + 急停 + 复位 + 事件）
- [x] D3 PLC 通信：行协议定义 + TCP 模拟器（下位机行为仿真）+ 异步客户端
- [x] D4 模拟相机 + 合成图像生成器（极柱车削纹理/环形焊缝鱼鳞纹/高光/噪声/四类缺陷形态）
- [x] D5 视觉算法（霍夫圆基准 → ROI 暗域焊缝质心 → 仿射换算偏差 → 四类判定）+ 九点标定（最小二乘 + 逆变换）
- [x] D6 图像流水线（BlockingCollection 有界反压 + 双 worker）+ Stop-and-Go 流程控制器
- [x] D7 主程序接线 + CSV 报表导出 + 标注图输出
- [x] D8 单元测试：状态机转移 / 标定恢复精度 / 算法闭环（真值 vs 实测）
- [x] D9 算法与剧本缺陷修复（见 R4 根因分析、R5 修复记录）
  - [x] D9.1 修复 BuildPlan 缺陷分布：`Dictionary.GetValueOrDefault(i)` 对未命中 key 返回 `default(WeldDefect)=Misaligned(0)`，导致 20 个 OK 点全部被赋予 1.2~1.7mm 超差真值（算法判定无误，是剧本错了）→ 改为 `TryGetValue + (WeldDefect?)null`
  - [x] D9.2 校准炸焊面积阈值：正常焊缝暗域实测 12.2k~13.6k px² > 原 `MaxWeldAreaPx=12000` → 全部误报 Blowout；调整为 16000（炸焊环+熔团 ≥ 27k，区分度充分）
  - [x] D9.3 测试断言精度修正：xUnit `Equal(precision)` 为舍入后比较，真值 −0.15 恰在舍入边界产生假失败 → 改显式误差断言 ±0.05mm
- [x] D9+ 卡尺亚像素精定位（计划外增强，见 R6）：霍夫圆整像素抖动导致 seed 4 误差 0.069mm 超容限 → 新增径向梯度卡尺(48 射线) + Kasa 最小二乘圆拟合，基准中心达亚像素精度
- [x] D10 全量回归（见 R7）：build 0 警告 0 错误；单测 **42/42 全绿**；主程序端到端 **24 点 = 20 OK + 4 NG@点位 3/7/11/15**，四类缺陷各命中一次、零误报零漏报，节拍 103ms/点
- [x] D11 README.md（使用说明 + 架构说明 + 真机接入指引）
- [x] D12 项目英文化落地：开发会话工作目录锁定原文件夹，Windows 无法重命名进程占用中的目录（见 R8），改为**完整复制到 `E:\c#code\BatteryWeldAOI`**（排除 bin/obj），并在新路径完成全套验证（见 R9）。原文件夹「新建文件夹 (2)」与本项目内容一致，可在关闭开发会话后手动删除。
- [x] P1-1 Core 健壮性加固（见 R10）
  - [x] P1-1a 到位等待超时（`ArrivalTimeoutMs`，默认 10s）：轴卡死/信号丢失时抛 `TimeoutException` 中止，不再无限轮询
  - [x] P1-1b 单指令应答超时（`PlcResponseTimeoutMs`，默认 5s）：检测 TCP 断线/假死
  - [x] P1-1c 流水线单点异常隔离：坏图/算法异常降级为 `HasError` 错误结果（带错误标注图），不击穿 worker，其余点位继续
  - [x] P1-1d 结果上传 PLC 失败自动重试 3 次，最终失败计入报表 `UploadFailedCount`
  - [x] P1-1e 操作员中止语义：取消 → 状态机 EmergencyStop(Stopped) → 排空流水线 → 部分结果报表（`WasCancelled`）
  - [x] P1-1f `RunAsync` 单次调用保护（每包检测独立控制器实例）
- [x] P1-2 参数配置化（见 R10）：`AppConfig` JSON 配方（点位数/种子/线程数/公差/超时/输出目录/存图开关），文件损坏回退默认值；计划生成器 `DemoPlanBuilder` 下沉 Core 供双入口共用
- [x] P1-3 WPF 桌面界面（见 R11/R12）：`BatteryWeldAOI.Wpf`（net8.0-windows，MVVM 手写零依赖）
  - [x] 实时标注图像（worker 线程转 BitmapSource + Freeze，UI 线程渲染）
  - [x] 点位矩阵 NG 高亮（绿=OK 红=NG 橙=异常 蓝=检测中，悬停显示单点偏移详情）
  - [x] 检测统计面板（总数/已检/OK/NG/异常/节拍）+ 进度条 + 用时
  - [x] 参数配方在线编辑与保存（运行中自动锁定）
  - [x] 开始/停止/复位/导出报表命令；运行日志时间戳 + 自动滚动
  - [x] UI 线程异常兜底：崩溃日志落盘 + 弹窗提示，不再静默消失
- [x] P1-4 单元测试扩充（见 R10/R11）：42 → **49 个**（流水线异常隔离 / PLC 到位超时 / 链路假死应答超时 / 错误结果上传载荷 / 配置回环与损坏回退）
- [x] P1-5 真实图像输入源（见 R13）：视频文件与摄像头检测
  - [x] `VideoFileGrabber`：视频逐帧取图（缩放至标定分辨率，帧数即点数，读穿/损坏抛明确异常）
  - [x] `CameraGrabber`：DirectShow 摄像头取图（设备索引可选，预热丢弃前帧等曝光稳定）
  - [x] `NullPlcClient`：无 PLC 离线模式（到位恒真），检测器零改动复用
  - [x] `AppConfig` 增加 InputSource / VideoFilePath / CameraDeviceIndex
  - [x] WPF 输入源面板：三选一切换、视频浏览（选文件即预读首帧验证解码）、摄像头实时预览开关
  - [x] 高级视觉参数开放编辑：极柱半径上下限 / 焊缝 ROI 半径 / 漏焊与炸焊面积阈值
  - [x] `tools/MakeDemoVideo`：用 ImageGenerator 合成演示视频（供视频模式验证）
  - [x] 单测：49 → **52 个**（合成视频回读帧数/缩放/内容、缺文件异常、NullPlc 即时到位）
- [x] P1-6 点位详情窗口（见 R14）：点击矩阵已完成格子弹出非模态详情窗口
  - [x] 标注大图懒加载（从已保存 PNG 按需解码，不常驻内存）、状态徽标（OK/NG/异常）
  - [x] 指标明细：缺陷类型、X/Y/合成偏移与公差对比（超差标红）、处理耗时、极柱/焊缝中心像素坐标
  - [x] 上一处/下一处导航（自动跳过未检点位，支持 ←/→ 键）、导出图片、Esc 关闭
  - [x] 非模态：不锁定主界面，连续点击不同格子直接切换；新一轮检测开始自动关闭
  - [x] 矩阵格子改为可点击按钮（悬停高亮；未完成点位不可点击）
- [x] P1-7 主界面布局修复（见 R15）
  - [x] 矩阵格子被裁剪/横向滚动条：全局 Button 样式 MinWidth=88 被格子按钮继承，8 列 × 88px=704px 撑爆 390px 面板 → 格子按钮显式 MinWidth=0
  - [x] 右面板 ScrollViewer 强制 HorizontalScrollBarVisibility=Disabled（杜绝横向滚动）
  - [x] 矩阵 GroupBox 标题过长 → 简化为"点位矩阵"，图例移入组内两行小字
  - [x] 高级参数组提示语换行破碎 → 随横向滚动修复 + TextWrapping 恢复完整
  - [x] 视频路径只读框加 ToolTip（悬停看完整路径）
  - [x] 删除"说明"GroupBox（信息已分散到各输入源提示），参数区行距 6→4，窗口默认高 860→890
- [x] P1-8 真实成像鲁棒性算法重构（见 R17）：消除全部绝对灰度阈值，算法不再"对着合成图调常数"
  - [x] 极柱定位：CLAHE(3.0, 8×8) 局部对比度归一化 + 霍夫圆（param1 120→70 兼容软边）；回退方案由"亮阈值 160"改为 Canny 边缘闭缺口 + 最大圆 + 圆度校验
  - [x] 焊缝检测：极柱盘内 patch（盘外中值填充消除极柱边缘干扰）→ 形态学闭/开背景差分 → 暗/亮双极性各做完整环检测 → 按「面积×覆盖率²」取最优
  - [x] 环形验证三重判据：Kasa 拟合半径 ∈ [0.25, 0.85]×极柱半径、角度覆盖率 ≥ 0.5、环带聚集度（0.75~1.25×拟合半径内像素占比 ≥ 0.7，剔除反极性假特征的实心团块）
  - [x] 噪声自适应阈值：残差下四分位单侧估计 σ（焊缝全在正侧、大缺陷不抬高阈值），T = max(25, 5σ)
  - [x] 断环回退只合并共圆分量（0.6~1.4×锚圆半径带内），拒绝无关碎块拼凑
  - [x] `NinePointCalibration.FitCircle`（Kasa）抽取为公共方法并修复半径公式符号错误（旧内联代码 r²=center²+c 应为 center²−c，此前仅用于标注所以未暴露）
  - [x] 新增 6 个配置参数（背景核/环半径比例/对比度下限/覆盖率/熔池面积阈值），随 config.json 持久化
  - [x] 对抗性 `RealisticImageGenerator`（测试程序集）：场景/光照双种子分离，注入随机曝光 0.7~1.4、光照梯度 ±25%、渐晕 0~25%、随机反照率、暗/亮焊缝极性、高光、脏污、σ=4~9 噪声；亮焊缝场景自动曝光保护（防焊缝饱和）
  - [x] 新增 43 个鲁棒性闭环测试：随机光照下四类缺陷检出与偏移测量、明暗极性、同场景不同光照不变性；单测 52 → **95 个全绿**
  - [x] 端到端零回归：24 点 = 20 OK + 4 NG（剧本点位 3/7/11/15 精确命中），节拍 101ms/点
- [x] P1-9 异常情况的显示与辨别修复（见 R18）
  - [x] 标注图文字支持中文（新增 `Vision/AnnotationText.cs`）：OpenCV Hershey 字体把中文逐字节画成 `?`，异常图整行变成 `ERROR: ????????????`
  - [x] 判定依据落进结果（`PointInspectionResult.ErrorMessage` 原约定未实现）：漏焊写下实测覆盖率/填充率与下限
  - [x] 报表新增 `Source`（原始文件名）与 `Reason`（判定依据）列，异常点可对应回文件并复查
  - [x] 偏移量无物理意义时（测量失败 / 未检出焊环）界面与报表一律显示 `—`，不再报 0.000mm
  - [x] 缺陷中文名收敛到 `WeldDefectText`（原先 WPF / 控制台各一份，标注图上还是英文枚举名）
  - [x] 焊环定位薄结构兜底路径：修掉环形光源反射细圈并块造成的**伪漏焊**
  - [x] 详情窗口增加「源文件」行，NG 显示判定依据；新增中文渲染 / 显示口径 / CSV 列单元测试与按需批量回归
- [x] P1-14 焊偏基准改为"孔位（端面灰盘）"（见 R24）：修掉"灰圆心被端面反光拉偏、偏移量系统性偏小"
  - [x] 新增 `Vision/HoleLocator`：按**递归 Otsu 分三层**定位孔位（暗缝隙 / 孔位端面 / 端面金属），
        取中间层。两个阈值都来自本图灰度分布，**不含绝对灰度常数**
  - [x] 焊偏基准改用它，梯度卡尺退为兜底（分层失败时不新增测量失败）
  - [x] 实测效果：用户指出的 Img023 由"孔位心 (2433,1827) r=199.4px、D=0.152mm"纠正为
        **(2305,1809) r=79.1px、D=0.651mm**（手工标定灰盘为 (2320,1811) r≈96）
  - [x] 批量误判率回归（960 帧厂商良品）：误判 NG **2 帧（0.21%）**、测量失败 1 帧（0.1%）——
        2 帧即修正后偏移量超过 1.0mm 公差的点
  - [x] **待工艺定夺**：`OffsetToleranceMm`=1.0 是按旧（偏小）口径留的。换正确基准后良品偏移量中位
        由 0.238 升到 0.262mm、p90 由 0.448 升到 0.514mm、最大 0.768→1.015mm，
        `PpSampleTests` 的"合格帧偏移量 ≤0.8mm"哨兵因此变红（**算法判定仍 24/24 全 OK**，是哨兵本身过期）。
        公差该定多少必须由工艺给，本项不擅自改
  - [x] **未完成**：B（焊环的点位中心，那个小暗圆）未实现——它需要该标记的**公称直径**作半径先验。
        实测其可见弧只占圆周 29%~47%，自由圆拟合严重病态（半径在 27~62px 之间乱跳），
        必须先有半径才能只反推圆心
- [x] P1-15 现场标定与焊偏公差落地（见 R25）：用户带卡尺到现场量了工件上的两个实物尺寸，以此为基准把
      **像素当量 0.025 → 0.1003**（0.0235mm/原图像素，视野 128.4mm），并把 **焊偏公差由 pp 参考集推出为
      6.6mm**、做成界面可调控
  - [x] 现场实测：凹陷圆环（焊环外那条细暗圈，工件上的凹坑沿）直径 **30mm**、孔直径 **5mm**，
        孔在凹坑中心（两者同心）；凹坑沿只在部分区域可见（是台阶阴影弧，不是每帧都有的完整圆）
  - [x] 定标依据：30mm 圆环在图上直径 1278 原图像素（先验证标注图裁剪与现场 JPEG 是 1:1：
        JPEG 8×8 块边界自相关在滞后 8 处 0.96，与现场原图自身对照的 0.98 同样尖锐）
        ⇒ 42.6px/mm ⇒ **0.0235mm/原图像素**；30mm 占图宽 23.4% ⇒ 视野 128mm（两条独立口径吻合）
  - [x] 旧值 0.025（配 1280 宽 = 视野 32mm）使**所有毫米读数偏小 4.01 倍**：真实的 1mm 偏移报成
        0.249mm，于是 1.0mm 公差物理上等于 4.0mm——这是"该判 NG 却判合格"的量级来源
  - [x] 消除像素当量的"四处各写一份"：新增 `NinePointCalibration.PureScale(mmPerPixel)`，
        `tools/VdbImport` 与现场数据测试改为从配置取值（此前只有 App/WPF 那一处跟着配置走，
        实测同一批图在界面与工具里报出差 4 倍的毫米数）
  - [x] `OffsetToleranceMm` 1.0 → **6.6**：20 SN / 480 帧厂商良品实测（修正当量后）min 0.198、
        p10 1.070、中位 1.989、p75 2.649、p90 3.780、p99 4.793、max 5.763mm → 取 max × 1.15；
        界面"焊偏公差 mm"输入框双向可改、随 config.json 持久化（`AppConfigTests` 的回环用例已覆盖）
  - [x] 验收：**40 SN / 960 帧厂商良品 100% 合格**（误判 NG 0、测量失败 0）
  - [x] 演示剧本的焊偏真值改由公差推出（×1.3）并夹到合成夹具可复现量程，不再写死 1.2~1.7mm
  - [x] **据实记录的两处后果**：① 按 6.6mm 判，12 帧厂商 NG 的偏移量（1.73~5.79mm）全部落在公差内
        ⇒ **焊偏判据在这批数据上零分辨力**（`NgSampleTests` 已改为只断言测量链正常并打印分布）；
        ② 合成夹具的毫米尺度跟着 `MmPerPixel` 走（同一份 `BuildCalibration` 服务所有输入源），
        当量 ×4 后演示基线由 21 OK/3 NG 变为 21 OK/2 NG/1 异常——用 config.json 覆盖回 0.025
        可精确复现旧基线（A/B 验证过），即漂移完全来自物理尺度
  - [x] **仍待定（必须与工艺定）**：这批良品的偏移量中位 2.0mm 到底是真偏心、还是"熔核中心"被
        新月形粗糙区带偏。反向证据：用户那帧的孔心与凹坑圆心只差 0.12mm（基准侧没问题），
        而粗糙熔核区质心离孔轴 3.5mm、单圆拟合 3.9mm——形状带偏的可能性不小。
        分辨办法：统计 480 帧偏移的**方向**（随机分布 = 真偏心；聚在固定方向 = 测量偏置）。
        另有一条独立佐证：孔位**半径**不可信（以 30mm 凹坑为尺，5mm 孔半径应为 106.5px，
        算法报 131.3px，偏大 23%——取的是影子外缘；真实孔沿落在阴影里量不到）
- [x] P1-13 熔核内沿与环宽（见 R22）：把"环形有厚度"落到可测量、可显示的**环宽**上
  - [x] `Vision/WeldInnerLocator`：在**纹理能量图**上按"粗糙起始半径"取熔核内沿。
        不能从掩码取——闭运算（0.045×base、两次迭代）已把约 20px 的极柱孔一起封死，
        实测后处理掩码是**实心盘**（OK_312 与 Img000 的"最大内切圆/等效半径"都是 0.97），内沿信息在掩码里不存在
  - [x] 搜索起点用 **1.15×灰圆半径**跳过极柱边缘带（极柱边缘是亮→暗阶跃，局部标准差同样高，会误触发）
        ——这正是 P1-12"灰圆独立测量"的成果复用
  - [x] 内外同心校验：偏差 > 0.05×外半径即不输出环宽（含反射细圈残留的帧实测偏差 48.8px），
        但**不判测量失败**——环宽只作诊断量，不参与判定
  - [x] `PointInspectionResult` 新增 `WeldInnerRadiusPx` / `WeldInnerCenterPx` / `WeldRingWidthPx`
        （含 Source 坐标版本）；标注图加画内沿圈（橙色，与外沿红圈区分）；详情窗口新增「熔核环宽」行
  - [x] 实测（tests/pp 前 20 个 SN / 480 帧）：**内沿 100% 取到**；内/外半径比中位 **0.374**（0.154~0.469）；
        环宽中位 **331px**（外半径约 528px，即环宽约占外半径 63%）；内外圆心偏差中位 1.3px
  - [x] 验证：单测 133 → **135 个、134 通过**（唯一失败仍是改动前即红着的 `NgSampleTests`）；
        核心指标（中心偏差分布、半径比）未受影响——环宽是纯增量
- [x] P1-12 焊环按环形识别 + 灰圆独立定位（见 R21）：把两个地标各自做准，并以它们为判定依据
  - [x] 并块修复：`FillHoles` 之后算圆度，会把"细圈围出的大空腔"伪装成圆度 0.98~1.00 的实心盘，
        再按填洞面积排序时压过真熔核（Img000 实测半径 706.6px vs 族值 530px、圆心偏 202.6px）。
        改为**族值带触发 + 只采纳落带结果 + 核宽阶梯 + 最接近族值/弧段支撑择优**
  - [x] 修掉核宽硬钳 `Geo.Odd(..., 3, 21)` 导致"核宽阶梯三级全被钳成同一个 21px、阶梯空操作"的隐蔽 bug
  - [x] 伪漏焊降级：激进路径找到候选但不可信时，把主路径的"确定没焊"降级为"测不出来交复核"，
        不再凭空造漏焊缺陷（实测 3 帧伪漏焊 NG → 测量失败）
  - [x] 灰圆（极柱）独立定位：尺度基准由**焊环半径**改为**灰圆自身族值半径 × base**
        （`PinRadiusFamilyRatio`），搜索窗/平滑/离群门限全部脱离焊环；射线原点只作初值，
        自迭代带两道实测闸门（半径 ≥12px 才迭代、残差须显著改善）
  - [x] 三项一致性校验（按"两个地标各自独立"的口径）：各自尺寸族值 / 各自拟合残差 /
        拓扑关系（极柱必须在焊环内，`D + R_pin < R_out`）；任一不达标判测量失败
  - [x] **明确不做同心约束**：焊环与灰圆之间的偏心量正是要测的焊偏，强行同心会把 D 抹平（漏检）
        或把良品全判成测量失败（误杀）
  - [x] 关键实测（tests/pp 前 20 个 SN / 480 帧，全部为厂商良品）：
        误判 NG **2 → 0**、未定位 **2 → 0**；偏差中位 11.1→10.9px、p90 **68.3→21.0**、
        p95 **126.2→61.1**、p99 **172.0→125.2**、最大 **215.3→156.8**、>100px 占比 **7.32%→2.08%**；
        半径比（我们/厂商）>1.25 占比 **10.46%→0.21%**；用户指出的 Img000 由 **202.6px → 6.4px**
        （半径 706.6→529.8px，回到族值 0.145×短边）
  - [x] 验证：`dotnet build` 0 警告 0 错误；单测 **133 个 / 132 通过**，唯一失败仍是改动前即红着的
        `NgSampleTests`
- [x] P1-11 毫米折算与像素坐标口径修复（见 R20）：把"数字看着对、实际会被误读"的两处口径问题收口
  - [x] `ToMillimetres` 改为**单一等比系数**：原先对 X 用 `ImageWidth÷图宽`、对 Y 用 `ImageHeight÷图高` 分轴折算，宽高比不一致时同一段物理距离在 X/Y 上被折成不同的毫米数。实测配置 1280×1024 跑 5472×3648 现场图，两轴系数 1.855 vs 2.226（**差 20%**），即偏移量的**方向**被歪曲 20%，而 1.0mm 公差判定正建立在这个合成值上
  - [x] `InspectionConfig.FovAspectDeviation(图宽, 图高)`：把"配置与图幅宽高比差多少"量化出来（返回 0.20 = 差 20%），实测值与旧两轴系数之比 2.226/1.855 完全一致；WPF 选图片文件夹时据此提示
  - [x] `PointInspectionResult` 新增 `SourceSizePx` 与 `WeldCenterSourcePx` / `PoleCenterSourcePx` / `WeldRadiusSourcePx` / `PoleRadiusSourcePx`：给界面与报表的是**原图坐标**而非内部工作分辨率坐标
  - [x] 详情窗口：坐标改原图口径并标注「（原图）」，两端**半径**一并显示（原先只给中心，操作员无法判断标注圈大小对不对），新增「图像尺寸」行写明原图与检测工作图的关系
  - [x] `VendorLocatorAgreement` 改用上述属性，去掉自己那份缩放逻辑（同一换算只应有一处实现）
  - [x] 新增 4 个测试：`OffsetScaleTests`（两轴同系数 / 原图坐标映射 / 合成图口径 / 宽高比偏差量化）
  - [x] 验证：`dotnet build` 0 警告 0 错误；单测 129 → **133 个、132 通过**（唯一失败仍是改动前即红着的 `NgSampleTests`）；实测 pp 图 kX = kY = 0.046377（旧实现会差 20%）

- [x] P1-10 现场厂商数据导入与定位精度量化（见 R19）：把 `tests/pp` 里厂商私有格式 `.vdb` 读通，用厂商自己的圆定位结果给"焊环定位"建一份零人工标注的参考基准
  - [x] `Diagnostics/VendorVdbReader`：解析 `.vdb` 头部（魔数 `BDV.`）取出厂商圆定位的圆心 X/Y 与半径；实测 62865 个文件全部解析成功（只读前 8192 字节，不解整张 JPEG）
  - [x] `Diagnostics/VendorVdbRecord` + `VendorLocatorAgreement`：厂商测量值模型，以及"工作分辨率 → 原图幅"的换算（工具与测试必须同口径，写错会差出一整个缩放比）
  - [x] `tools/VdbImport`（已加入解决方案）：`export` 全量导出厂商测量值 CSV；`agree` 做本项目定位 vs 厂商定位的横向体检，输出逐帧偏差与最差帧清单
  - [x] 定位精度回归 `PpLocatorAgreementTests`：把"定位准不准"从间接指标（不误判）升级为可量化断言（偏差中位数 ≤20px、定位失败率 ≤5%）
  - [x] 实测基线：前 20 个 SN / 478 帧，偏差中位 **11.1px**（p90 67.4 / p99 171.4 / 最大 215.3，焊环半径约 528px）、定位失败 **0.42%**
  - [x] 参照系（决定"优化到什么程度算够"）：厂商自己在**同一张图**上换三个位置标记（Pos0/1/2）的圆心互差中位就有 **24.4px**（20955 帧统计）——本项目定位一致性已优于厂商自身复现能力，继续贴合厂商只会拟合其噪声
  - [x] 关键结论（已用测试钉住）：`.vdb` 内嵌 JPEG 与 `Orignal_pic` 同名 jpg **逐像素完全相同**，即"原图"实为厂商 GUI 渲染输出（带叠加标注、JPEG 质量 1 / 0.105 bit/px），不是原始相机帧
  - [x] 边界记录：厂商判定值**不在文件里**（`焊偏距离` 在 20955 个 Pos0 文件中 20954 个是 0.000 模板占位；无 OK/NG 状态字段，40 OK + 40 NG 逐字节比对候选数为 0），故判定逻辑与公差**无法从这批数据反推**，仍须向厂商索要工程文件/数据库导出 + 工艺真值（标定件实测 mm/px 与已知偏移试焊）

### 后续路线图（Phase 2/3，本仓库之外的方向）

- [ ] P2-1 ~~WPF 界面~~（**已于 P1-3 完成**）→ 剩余：多语言、配方管理多文件、历史记录浏览
- [ ] P2-2 真机接入：海康/Basler SDK 实现 `ICameraGrabber`；Modbus TCP/S7 实现 `IPlcClient`
- [ ] P2-3 数据库落盘（SQLite/PostgreSQL）+ MES 对接
- [ ] P2-4 断点续检：从故障/中止点位继续，而非整包重跑
- [ ] P3-1 深度学习缺陷分类：ROI 裁剪 → ONNX Runtime 部署 YOLOv8（传统定位 + AI 判缺）
- [ ] P3-2 飞拍（Flying Capture）：运动控制卡位置比较触发 + 全局快门短曝光

> **现场化计划（N1–N6）见 `FIELDPLAN.md`**：分配置存储 / 黑环帧焊环识别 / 工艺真值与缺陷类型 /
> 相机 SDK 接入 / 现场定点拍摄 / px↔mm 光学换算。与上面 P2-1、P2-2 有重叠，**编排顺序与对外提问清单以该文为准**。

## 六、执行记录

- **R1**（D1~D7，2026-09-11）：完成骨架→主程序全部编码。首次构建 6 个 OpenCvSharp 4.10 API 兼容性错误（`HoughCircles` 新签名返回 `CircleSegment[]`、`Cv2.Randn` 大小写、`Point2f→Point2d` 等），已逐一修复，构建 0 警告 0 错误。
- **R2**（D8，2026-09-11）：首轮单测 34/42 通过。四类缺陷检出全部正确；不合格项集中在 OK 点误报（Blowout/Misaligned）与测量精度断言过严。
- **R3**（诊断，2026-09-11）：`tools/Diag` 单线程复现主程序路径。**算法测量精度验证通过**：真值 (−0.625, −1.106)mm vs 实测 (−0.600, −1.102)mm，误差 ≈1px；焊缝环连通性良好（连通域总数=1）。
- **R4**（根因定位，2026-09-11）：定位两个根因——① 剧本生成 `GetValueOrDefault` 默认值陷阱（见 D9.1），OK 点真值本身就是超差的，算法「误判」其实判对了；② 正常焊缝面积阈值过低（见 D9.2）。结论：**算法无需改动，改剧本与阈值**。
- **R5**（D9.1~D9.3 执行，2026-09-11）：`Program.cs`/`tools/Diag` 改用 `TryGetValue`；`MaxWeldAreaPx` 12000→16000（附实测依据写入注释）；测试改显式 ±0.05mm 误差断言。回归 41/42。
- **R6**（D9+ 卡尺精化，2026-09-11）：剩余 1 个失败（seed 4，Y 误差 0.069mm）源于霍夫圆整像素抖动。新增 `RefineWithCaliper`：Sobel 梯度 + 48 径向射线在粗半径 ±12px 窗口找最强边 + Kasa 圆拟合（复用 `NinePointCalibration.Solve3`，已开放为 internal）。这与需求文档中「找圆卡尺找极柱中心」方案一致。回归 **42/42 全绿**。
- **R7**（D10 端到端验收，2026-09-11）：主程序运行 24 点：**OK 20 / NG 4**，NG 精确落在剧本点位 3(焊偏 D=1.501mm)/7(漏焊)/11(炸焊)/15(飞溅)，无误报无漏报；OK 点最大偏移 0.369mm（公差 1.0mm 内）；整包节拍 2.48s（103ms/点 ≈ 轴运动 60ms + 曝光 8ms + 流水线并行消化算法耗时）；CSV 报表与 24 张标注图已导出。退出码 2 = 存在 NG（按设计）。
- **R8**（D11/D12，2026-09-11）：README.md 完成。文件夹重命名被阻塞——开发代理会话工作目录位于文件夹内，Windows 拒绝重命名占用中的目录（已排查并排除：dotnet build-server、MSBuild 节点、testhost 等子进程）。保留会话外手动执行方案（见 D12），重命名后项目零改动可用。
- **R9**（D12 落地，2026-09-11）：采用完整复制方案落地英文名：`robocopy`（排除 bin/obj）复制到 `E:\c#code\BatteryWeldAOI`。**新路径全套验证通过**：`dotnet build` 0 警告 0 错误；`dotnet test` **42/42 全绿**；端到端 24 点 = 20 OK + 4 NG（焊偏/漏焊/炸焊/飞溅各 1，与剧本点位 3/7/11/15 精确对应），节拍 98ms/点，CSV 与标注图正常导出。证明项目与绝对路径无关，符合"重命名即用"预期。
- **R10**（P1-1/P1-2，2026-09-13）：健壮性加固 + 配置化。`TcpPlcClient` 增加到位等待超时与单指令应答超时（均抛明确 `TimeoutException`）；`ImagePipeline` 单点异常隔离（坏图 → `HasError` 错误结果 + 错误标注图，worker 存活）；`InspectionController` 上传重试 3 次 + `UploadFailedCount`、取消 → EmergencyStop + 部分报表、`RunAsync` 单次调用保护；`PointInspectionResult` 增加 `HasError/ErrorMessage`，`InspectionReport` 增加 `ErrorCount/WasCancelled/UploadFailedCount`；新增 `AppConfig`（JSON 配方，损坏回退默认）与 `Planning/DemoPlanBuilder`（计划/标定构建下沉 Core，控制台与 WPF 共用）；控制台 `Program.cs` 改读 `config.json`，CLI 参数可覆盖。回归：新增 7 测试，**49/49 全绿**。
- **R11**（P1-3/P1-4，2026-09-13）：WPF 界面首版。手写 MVVM（`ObservableObject`/`RelayCommand`/`PointItemViewModel`/`MainViewModel`），`MatConverter`（Mat→BitmapSource，像素复制+Freeze 跨线程）；MainWindow 布局：工具栏/进度条/实时图像/统计面板/点位矩阵/参数配方/日志。**实机验证**（真实启动+UI 自动化点击）：24 点整包 OK20/NG4/节拍 2.4s；点位矩阵像素级验证 20 绿 + 4 红；参数编辑生效（轴耗时 400ms → 实测 438ms/点）；运行中停止 → 状态"已停机" + 部分报表 `*_stopped.csv` 自动导出。
- **R12**（P1-3 修复，2026-09-13）：两处实机验证发现的缺陷修复——① `MatConverter` 中 `using var src = mat.IsContinuous() ? mat : mat.Clone()` 在连续 Mat 时把调用方的标注图误释放（改为仅克隆件归转换器释放）；② 日志 `ScrollIntoView` 在 `CollectionChanged` 内同步调用，批量追加触发 ItemsControl 项生成器不一致崩溃（延迟到 `DispatcherPriority.Background`），并增加 `App.DispatcherUnhandledException` 兜底（崩溃日志落盘 + 弹窗）。修复后整包运行、停止、复位全部复验通过。
- **R13**（P1-5，2026-09-13）：真实图像输入源落地。新增 `VideoFileGrabber`/`CameraGrabber`/`NullPlcClient` 与 `InputSource` 配置，WPF 输入源面板（切换/浏览/预览）与高级视觉参数面板，`tools/MakeDemoVideo` 视频样本生成器。**实机验证**：① 视频模式——MakeDemoVideo 生成 24 帧 AVI，逐帧检测 OK20/NG4，缺陷判定与仿真模式一致（Frame003 焊偏/007 漏焊/011 炸焊/015 飞溅），节拍 36ms/点（无 PLC 开销）；② 摄像头模式——本机 DirectShow 相机 #0 实时预览 + 24 帧实时检测全链路跑通（真实桌面画面无焊缝，全部正确判 MissingWeld，零异常零崩溃）。单测 52/52 全绿。
- **R14**（P1-6，2026-09-13）：点位详情窗口。`PointItemViewModel` 挂载完整结果与标注图路径，`PointDetailWindow` 非模态展示（大图懒加载/指标明细/导航/导出），矩阵格子改可点击按钮。首版用 Command+RelativeSource 绑定，实机点击无响应（绑定静默失败），改 Click 事件路由后通过。**实机验证**：点 03 格子弹出 Frame003 详情（焊偏 NG、D=1.491mm 超差、图像像素级确认已显示）；「下一处」切到 Frame004 OK；不关窗口直接点主窗口 07 格子切换到 Frame007 漏焊（"未检出焊缝"）——非模态切换符合设计。
- **R15**（P1-7，2026-09-13）：主界面布局修复。截图审查发现 7 处布局问题，根因最大者是全局 Button 样式（MinWidth=88/Padding=14,6）被矩阵格子按钮隐式继承 → 8 列格子 704px 撑爆 390px 面板，第 8 列被窗口边缘裁掉且出现横向滚动条。修复：格子按钮 MinWidth=0、ScrollViewer 禁用横向滚动、矩阵标题简化+图例移入组内、视频路径加 ToolTip、删除说明组、行距压缩、窗口默认高度 890。期间 PowerShell 改写 XAML 触发 UTF-8/GBK 双重编码损坏，整文件重写恢复。**实机验证**：全部 24 格完整可见（AX 边界确认第 8 列右缘 2074 < 面板右缘）、无横向滚动条、提示语完整换行；跑一轮检测后点击此前被裁剪的 23 号格子弹出详情正常。52/52 测试全绿。
- **R16**（用户反馈修复，2026-09-13）：用户报告"点击红色格子报错"。crash.log 定位：`InvalidOperationException: 关闭窗口后无法调用 Show` —— WPF Window Close 后对象销毁不可复用，而主窗口持有旧引用（`??=` 只创建一次），详情窗口关闭（×/Esc/关闭按钮/新一轮检测自动关闭）后再点格子即在已关闭窗口上调用 Show。修复：订阅详情窗口 `Closed` 事件清空引用，下次点击重建新窗口。**实机复现验证**：点 03 开窗→点关闭→再点红色 07 格子，窗口干净重开（Frame007），无报错，crash.log 零新增。
- **R17**（P1-8，2026-09-14）：真实成像鲁棒性算法重构。背景：原算法焊缝判定的固定阈值（暗阈值 85、亮阈值 160、面积常数）全部是对着 ImageGenerator 调出来的，真实图像上必然失效。重构后算法**不含任何绝对灰度阈值**：CLAHE 归一化霍夫 + 卡尺亚像素基准定位；极柱盘内形态学背景差分 + 暗/亮双极性环检测（面积×覆盖率²择优）+ 三重环验证（半径比例/角度覆盖/环带聚集度）+ 下四分位自适应阈值。配套 `RealisticImageGenerator` 对抗性生成器（场景/光照双种子：曝光 0.7~1.4、梯度 ±25%、渐晕、随机反照率、明暗极性、脏污、噪声）。**调试过程中定位并修复三个深层缺陷**：① `FitCircle` 半径公式符号错误（r²=center²+c，旧代码仅用于标注绘图故长期潜伏，验算 1172≈√(center²+2center²−r²) 精确吻合）；② 开/闭运算对"核大于环内孔"的环存在内区破坏效应，在反极性图上产生大面积假特征——由环带聚集度校验 + 共圆合并约束防御；③ σ 四分位距估计被大缺陷污染（炸焊占盘内 40% 面积时阈值飙到 300 全漏检）——改下四分位单侧估计。首轮回归 61 个失败，经 Diag 工具逐种子诊断后修复；最终 **95/95 全绿**（52 原有 + 43 新增），端到端 24 点 = 20 OK + 4 NG 与基线完全一致（节拍 101ms/点）。已知局限：亮焊缝场景强镜面高光与亮飞溅不可分，需偏振消光光源（成像前提，已写入 README）。
- **R18**（P1-9 异常显示与辨别修复，2026-09-15）：用户报「tests/pp 现场目录里的异常标注图显示成一串『?』」，并要求完善异常情况的显示与辨别；随后又报「Img010 明明是完好焊环却判漏焊」。四处根因与修复：
  1. **标注图文字画不出中文**（"?"的直接原因）：结论文字一直由 `Cv2.PutText`（Hershey 字体）绘制，而 Hershey 只覆盖 ASCII——非 ASCII **逐字节**被画成 `?`。又因只有"异常"分支的文字带中文（`ERROR: {中文原因}`），于是**恰好只有异常那几张图**整行变成 `ERROR: ????????????`：详情窗口里写着原因、图上却只有问号。新增 `Vision/AnnotationText.cs`：Windows 下用 GDI+ 把文字渲染到**文字所在的小位图**再写回 Mat（不做整图转换，20MP 图上开销可忽略；带半透明深色描边，亮金属上的绿/红细字原本几乎看不清），非 Windows 退化到 Hershey 并把画不出的部分显式折叠为 `*` 占位（绝不画问号冒充内容）。异常文案同时改为中文短原因（`异常：焊环边界支撑弧段不足`，完整诊断留给界面与报表），缺陷名统一中文（`NG（漏焊）`）。
  2. **判定依据没落进结果**：`PointInspectionResult.ErrorMessage` 本就约定"漏焊判定依据写这里"，但代码只在测量失败时填。现在漏焊写下实测值与下限（`环弧覆盖率 62% < 下限 75%`），报表新增 `Reason` 列与 `Source` 列（原始文件名）——异常点终于能对应回具体文件并复查；详情窗口增加「源文件」行、NG 显示判定依据；缺陷中文名收敛到 `WeldDefectText`（原先 WPF/控制台各一份、标注图上还是英文枚举名）。
  3. **偏移量显示口径误导**：测量失败与"未检出焊环"的漏焊，偏移量恒为 0.000mm 且被原样显示，操作员会读成"焊得很正"——最该人工复核的点位恰好读反。新增 `HasValidOffset`，此类点位界面与报表一律显示 `—`；同时修掉 NG（漏焊/炸焊/飞溅）显示"（公差内）"的自相矛盾。
  4. **伪漏焊（算法）**：Img010（19-22-22）的纹理掩码被环形光源反射细圈（约 20px 厚）与熔核桥接成**一整块 79535px、横跨可用区并贴边**的连通域，被"贴边连通域=台面/背景"的剔除规则整块丢弃 → 候选数 0 → 报"未找到纹理显著区域（无焊点）"漏焊。修复：`WeldRingLocator.Detect` 的薄结构删除改为按核宽参数传入，`WeldInspectionAlgorithm` 增加**兜底路径**——主路径（保守）**完全找不到焊环时**才用 `InspectionConfig.FallbackPreOpenKernelRatio`（0.045×base）在闭运算前把细圈开运算掉重试一次。`PreOpenKernelRatio` 默认仍为 0（主路径行为不变）：掩码本身稀疏的点位主路径就能成功，根本不会走到兜底路径，故避开了该项文档里记录的误伤（tests/tp 22.png 会变测量失败）。
  **验证**：`dotnet build` 0 警告 0 错误；单测 121 → **122/122 全绿**（新增 11 个：中文不落成问号（与等长问号串逐像素比对）/ 端到端异常标注图 / 短原因 / 偏移量不适用 / 缺陷中文名 / CSV 源文件列与转义 / Hershey 降级不含问号；另加按需批量回归 `PpBatchRegressionTests`，用环境变量开启）。`tests/pp` 该 SN 由 22/24 → **24/24 帧合格**（19-22-07：测量失败 → OK D=0.357mm；19-22-22：伪漏焊 → OK D=0.094mm，环r/短边=0.1430 回到同批家族值 0.1448，中位半径比不变）。WPF 实机复跑同一目录：**OK 24 / NG 0 / 异常 0**，节拍 258ms/点，标注图为中文。
  **残留（据实记录）**：批量回归扫 60 个 SN / 1440 帧 → **误判 NG 10 帧（0.69%）、测量失败 0**；10 帧全部是"完好焊环 + 反射细圈"的同因残留（`Reason` 列已给出具体判据：未找到纹理显著区域 / 未找到焊环（最大圆形纹理区仅 2.6~5.6%×短边）/ 纹理区域均不呈圆盘状），即经 0.045 兜底清理后形状仍未通过环形校验。继续放宽清理核宽有可能修掉它们，但**本仓库没有真实 NG 样本，无法验证"更强清理会不会漏检真实缺陷"**，故不擅自放宽，待采集到现场 NG 样本后再标定。

- **R19**（P1-10 现场厂商数据导入，2026-09-16）：用户问「pp 里的 .vdb 是不是读不了、不能当参考数据」，随后要求「先把能做的做了」。原判断（`PpSampleTests` 类注释：厂商私有格式、无公开规格、未纳入测试）只停留在识别魔数，本轮把格式读通并落地为可复用的验证能力。
  **格式**：每个 `.vdb` = 4 字节魔数 `BDV.` + 一段**序列化的 GUI 文档对象图 dump**（内含原始堆指针，故同模板文件在指针字节上随机不同）+ 一整张嵌入 JPEG（`FF D8 FF` .. `FF D9`）。头部长度**随位置标记变化**：Pos0 约 3461 字节，Pos1/Pos2 实测 103794 / 720218 字节——最初把「头部 3461 字节」写死进 JPEG 定位逻辑，导致 41910/62865 个文件（全部 Pos1/Pos2）解析失败，改为「测量值解析必须与 JPEG 定位解耦」后全部通过。中文以 **GBK** 存储（纯 ASCII 扫描看不到关键信息），按 GBK 解出的工具链标签为 `图像预处理结果区域` / `区域圆定位_New Result1` → `结果1_圆` / `直线创建_New Line1` / `焊偏距离`，据此确认厂商判定的物理量正是焊偏。圆心 X/Y 与半径以 IEEE double 存在头部；半径用「头部里与字符串已知 X/Y 相符的连续三元组」定位，不写死偏移。
  **三个实测结论**（决定这批数据能做什么、不能做什么）：
  ① 内嵌 JPEG 与同名 `Orignal_pic/*.jpg` **逐像素完全相同（最大差值 0）**，且 Pos0/1/2 三个文件内嵌的是同一张图（差值为 0）→ 所谓「原图」实为厂商 GUI 渲染输出（带叠加标注），Pos1/Pos2 也不提供额外图像；该图是 JPEG 质量 1（0.105 bit/px、量化表全 255、平坦区 8×8 块边界梯度为非边界的 17.3 倍），亚像素信息已被破坏，**不能当毫米级测量的基准**。
  ② 厂商判定值**不在文件里**：`焊偏距离:0.000` 在 20955 个 Pos0 文件中 20954 个是 0.000（模板占位，唯一例外为 1.422）；用 40 个 OK + 40 个 NG 逐字节找「OK 组恒为一个值、NG 组恒为另一个值」的字节，候选数为 0 → 判定结论只存在于文件名。故判据与公差**无法反推**。
  ③ 厂商圆定位圆心**可用作参考基准**：与本项目焊环中心对齐比较（前 20 个 SN / 478 帧，由 `tools/VdbImport agree` 产出）偏差中位 **11.1px**、p90 67.4、p99 171.4、最大 215.3，焊环半径约 528px（中位偏差约 2.1% 半径）；而厂商自己在同一张图上换三个位置标记的圆心互差中位就有 **24.4px**（20955 帧统计）→ 本项目定位一致性已优于厂商自身复现能力，「贴合厂商」不能当收敛目标（继续压只会拟合其噪声），真正该盯的是 0.42% 的定位失败。厂商定位半径中位 482px，与本项目 528px 系统性差 9%（两者取的环边界不同），不可互相替代。
  **一条未定论的线索**：厂商结果值 D（钳在 ±7.425、可为负、语义未定）与厂商 NG 存在关联——12 个 NG 帧中 3 个 D 撞到钳位，NG 组饱和率 25.0% vs OK 组 4.47%（富集 5.6 倍，Fisher 精确检验双侧 p=0.0146）。方向上支持「这 12 个 NG 更可能是厂商测量不可靠标记而非缺陷分类结论」（与其几何量落在良品分布中部、每张焊环完整这两点自洽），但 n=12 且反向解释亦成立，需厂商判据才能定论。
  **交付**：新增 `Core/Diagnostics`（读取器 / 记录模型 / 一致性比较）、`tools/VdbImport`（`export` + `agree`，已入解决方案）、5 个测试（文件名约定解析、非 VDB 拒绝、真实语料 60 文件字段合理性、内嵌图与原图逐像素一致、定位精度回归）。`dotnet build` 0 警告 0 错误；单测 **124 → 129 个、128 通过**，唯一失败仍是改动前即红着的 `NgSampleTests`（12 张厂商 NG 中 11 张被判合格——正是本轮结论所指的「标签判据未知」，非本次改动引入）。全量导出已跑通：**62865/62865 解析成功、零失败**，产物 `.scratch/vendor_vdb.csv`。
  **未做（需外部输入）**：① 判定逻辑与公差标定——需厂商工程文件/数据库导出 + 工艺真值（标定件实测 mm/px 与已知偏移试焊），无此不能碰判定层；② 修正 `NgSampleTests` 的红色——它断言「12 张厂商 NG 一张都不能判合格」，而现有证据说明该断言的前提（NG 标签即缺陷真值）不成立，但在拿到厂商判据前不擅自改断言；③ 用厂商圆心做定位弱监督训练——精度已优于厂商复现能力，收益上限低，应让位于判定层的真值采集。

- **R20**（P1-11 毫米折算与坐标口径修复，2026-09-16）：用户看点位详情窗口时问「识别的内容和要求一样吗？为什么标注的圈和实际内容有所偏离」。先复现该帧（`tests/pp` 的 `19-22-36 730_..._OK_312.jpg`，即窗口里的 Img019），用三重独立验证确认**圈没画错**，再修掉顺着查出来的两处口径问题。
  **圈的准确性（三重验证，全部通过）**：① 从保存的标注 PNG 里按精确色值反解画出的圆 → 圆心 (2467.97, 1815.98)、半径 525.68、拟合残差中位 2.26px，与算法报告的 (2468.3, 1816.1) r=526.3 一致到 0.6px（**渲染与坐标缩放无偏差**）；② 独立测量（纹理掩码质心 (2473.4, 1823.6)、等效半径 530.8、最大内切圆 r=515.9）与我们的圆心差 **9.1px**；③ 厂商 VDB 的圆定位 (2474.9, 1828.8) 与我们的圆心差 **14.3px**（焊环半径 526px，即 2.7%）。极柱圈同样吻合：反解 r=172.98，而极柱端面实测边界在 r≈170~175（亮度从 r=155 的峰值 234 掉到 r=175 的 195、r=195 的 122）。
  **用户看到的"偏离"来自三处，都不是定位错误**：① 熔核本身不是圆的（螺旋状、右侧带凹湾），任何单一圆都贴不住——圈是拟合模型而非轮廓；② 极柱绿圈外那圈暗环是**极柱与熔核之间的缝隙/孔壁**，不是极柱，绿圈标的极柱端面边界是对的；③ 我们的环半径定义与厂商差 9%（526 vs 483px，取的环边界不同）。
  **顺带查出并修掉的两处口径问题**：
  1. **毫米折算的轴向偏斜（真缺陷）**：`ToMillimetres` 原按 X/Y 分轴折算，配置 1280×1024 跑 5472×3648 现场图时两轴系数 1.855 vs 2.226，**差 20%**——物理上"毫米/像素"是成像系统属性，不该随轴向变化，而 1.0mm 公差判定正建立在被歪曲的方向上。改为单一等比系数（取宽边之比，与配置注释里 X 轴原本的口径一致）。新增 `FovAspectDeviation` 量化配置与图幅的宽高比偏差并接入界面提示。
  2. **界面显示内部坐标（会被误读）**：详情窗口原样显示 `WeldCenterPx`/`PoleCenterPx`，那是**归一化后的工作分辨率坐标**（20MP 现场图上与显示值差约 8 倍），操作员拿它去图上核对量不到，"标注圈是不是偏了"无法自查。`PointInspectionResult` 新增 `SourceSizePx` 与原图坐标属性，界面坐标改原图口径、标注「（原图）」、补上两端半径与「图像尺寸」行。
  **验证**：`dotnet build` 0 警告 0 错误；单测 129 → **133 个、132 通过**（唯一失败仍是改动前即红着的 `NgSampleTests`，即 R19 记录的"厂商 NG 判据未知"）；`PpSampleTests` 的偏移量上限断言与 `PpLocatorAgreementTests` 均照旧通过；实测 pp 图两轴系数 kX = kY = 0.046377（旧实现为 1.855/2.226，差 20%）；控制台端到端 24 点跑通、CSV 与标注图正常导出。
  **新发现（据实记录，本次未改）**：控制台仿真剧本的验收基线已与 `DEVPLAN` 记录的 R7/R17 不符——现在是 **21 OK / 3 NG（漏焊×1、炸焊×2）**，而记录是 20 OK / 4 NG（焊偏/漏焊/炸焊/飞溅各一）。逐点实测：点位 3（剧本焊偏）测得 **D=0.623mm → 判合格（漏检）**；点位 11 炸焊（对）；点位 15（剧本飞溅）现在报**炸焊**。**与本轮改动无关**：仿真路径的原图与配置宽高比一致（工作图 575×460 恰为 1280×1024 的等比缩放），新旧两种折算在该路径上算出的系数完全相同（1280/575 = 1024/460 = 2.226087），且飞溅/炸焊判据根本不涉及毫米换算。根因方向：`DemoPlanBuilder.BuildPlan` 把焊偏点的真值距离设为 **1.2~1.7mm**（`1.2 + rng*0.5`），而 `InspectionConfig.PinSearchInnerRatio` 与 `OffsetToleranceMm` 的注释已写明可测上限仅约 **0.8×孔洞半径 ≈ 0.8mm**，超出后算法会"报一个偏小的偏移量从而误判合格"——即剧本本身要求了一个超出量程的工况。该漂移应在 R18 算法重构时引入（R18 只复验了 pp 目录，未复验仿真 24 点基线）。**这暴露的是"1.0mm 公差 > 0.8mm 可测量程"这一内在矛盾**，修法有三条（改剧本距离 / 改算法量程 / 改视野与公差），属工艺与光学决策，不在本轮范围内。

- **R21**（P1-12 焊环按环形识别 + 灰圆独立定位，2026-09-16）：用户先指出"焊环作为环形时有厚度，应该用环形或两个圆来匹配"，随后指出"标注的圈和实际内容有偏离"（Img000），最后纠正了本方案的一处**物理模型错误**。据此落地。
  **被纠正的模型错误（据实记录）**：我最初的方案让"焊环外边界"与"灰圆"共同约束同一圆心并强求同心。用户指出这在物理上不成立——**熔核的内沿与外沿是同心于激光中心的（同一对象的两条边），而极柱是盖板上的独立固定零件**；激光打偏时焊道内沿与极柱边缘会错位甚至交叠。强行同心会直接把待测量 D 抹平（漏检）或把良品全判成测量失败（误杀）。修正后的模型：`C_weld`（熔核自身）与 `C_pin`（灰圆）**各自独立测量**，`D = ‖C_weld − C_pin‖`，两者之间不加任何同心约束。
  **并块根因与判据的实测结论**：
  ① 形状与密度统计量**全部失效**——实测伪候选与真熔核在面积密度（raw/filled）上都是 1.000、填洞后圆度都是 0.98、掩码内低能量占比只差 2.5 倍（11.5~14.1% vs 3.7~5.8%）。原因是闭运算在几何上把稀疏材料填实了（Img000 的后处理掩码是**单个实心连通域**、面积 1,573,029 ≈ 半径 707 的整盘；而"先开运算再闭运算"只剩 956,090 = 熔核）。
  ② **唯一能干净分开两者的是半径族值带**：良品 0.142~0.146×短边，伪候选 0.193~0.200（6 个不同 SN 实测 91.0~91.9px ÷ base 460）。
  ③ 由此把"是否值得重试激进路径"的触发条件从"**完全找不到**焊环"扩为"**半径落在族值带外**"——旧条件永远命中不到第二种形态（主路径"看似成功"地给出了伪候选）。
  **两处必须分开的上限（踩过坑）**：起初把有效性上界从 0.32 直接收到 0.18，导致 **62 个单元测试失败**——因为**炸焊本来就靠"熔核明显偏大"检出**（`BlowoutRingRadiusRatio`=0.26、合成夹具按 0.275 渲染）。最终把两者拆开：`MaxRingRadiusRatio`=0.32 仍作**有效性**边界（超出判测量失败），新增 `RingRadiusFamilyMaxRatio`=0.18 只用于**触发重试**与**接受重试结果**。
  **另一处隐蔽 bug**：`WeldRingLocator` 把薄结构删除核宽硬钳在 21px（`Geo.Odd(..., 3, 21)`），于是核宽阶梯的三级（21/27/34px）全被钳成同一个 21px——**阶梯实际是空操作**（加阶梯后全量指标一字未变才发现）。放宽上限到 0.11×base 后阶梯才真正生效。
  **灰圆独立定位的两道闸门（都是实测踩出来的）**：搜索尺度改用灰圆自身族值半径后，自迭代在两侧表现相反——合成夹具的灰圆约 16.8px，迭代显著改善大偏移（D/q≈0.9）下的圆心（不迭代时把真值 0.70mm 量成 0.865mm）；而 tests/tp 的 15.png 灰圆只有约 10.7px，迭代反而把偏移量从 0.674mm 翻成 1.295mm 的伪"焊偏"。最终用两道闸门分开：**灰圆半径 ≥12px 才迭代**（重锚定的收益取决于盘沿跨多少个采样点，是绝对像素尺度问题）+ **只采纳残差显著改善的后续轮**。搜索窗外边界也从 1.90 收到 1.55（按"与原 `0.55×焊环半径` 等效"重新标定，tp 的 0.13 环径比下原窗口更窄，开宽了会把暗环卷进来、把孔/环量成 0.446 超出良品上界 0.38）。
  **验证**：`dotnet build` 0 警告 0 错误；单测 **133 个 / 132 通过**（唯一失败仍是改动前即红着的 `NgSampleTests`；期间修了 `AppConfigTests` 的两处——它按设计在新增 `double[]` 属性时抛异常提示补构造，且数组必须按序列比较而非引用相等）。tests/pp 前 20 个 SN / 480 帧全部为厂商良品：**误判 NG 2→0、未定位 2→0**；偏差中位 11.1→10.9px、p90 **68.3→21.0**、p95 **126.2→61.1**、p99 **172.0→125.2**、最大 **215.3→156.8**、>100px 占比 **7.32%→2.08%**；半径比 >1.25 占比 **10.46%→0.21%**（最大 1.517→1.281）；用户指出的 Img000 由 **202.6px → 6.4px**。
  批量误判率回归（40 个 SN / 960 帧，全部厂商良品）：**误判 NG 6 帧（0.63%）→ 0 帧（0.00%）**；测量失败 0 → 1 帧（0.1%，原因是"孔洞半径异常 10.0%×焊环半径，期望 12%~45%"——该帧灰圆量得太小，判人工复核而不是给出一个错读数）。
  **残留（据实记录）**：① 偏差尾部仍在（>100px 占 2.08%、最大 156.8px），但半径比最大仅 1.281，说明这些帧不再是"锁错对象"，而是熔核本身非圆（螺旋/带缺口）时单圆拟合的固有分歧；要再进一步需要按环形做真正的双边界拟合（本轮未做，见下）。② 本轮**未做** Coverage/Fill 的环带化重定义（会改变漏焊判定，需重新标定阈值）。③ 熔核**内沿**未取到：实测 21px 闭运算已把约 20px 的极柱孔封死（OK_312 的"最大内切圆/等效半径"=0.97、Img000=0.97，都是实心盘），因此"环宽"这个量本轮没有落地——它是下一步。

- **R22**（P1-13 熔核内沿与环宽，2026-09-16）：承接 R21 未落地的"环宽"。R21 里发现熔核内沿**在掩码中已被闭运算封死**，本轮换成在纹理能量图上直接取"粗糙起始半径"。
  **方法**：从焊环圆心向外，沿射线在纹理能量图上找第一次**持续**超过二值化阈值的半径（射线内做约 5px 移动平均抑制鱼鳞斑内部噪声）。搜索起点取 **1.15×灰圆半径**——必须跳过极柱及其边缘带，因为极柱边缘是亮→暗阶跃、局部标准差同样很高，从盘内起搜会把它误当成内沿；极柱半径现在可信（R21 的灰圆独立测量），这个起点才成立。
  **实测（tests/pp 前 20 个 SN / 480 帧）**：内沿**100% 取到**；内/外半径比中位 **0.374**（范围 0.154~0.469）；环宽中位 **331px**（外半径约 528px → 环宽约占外半径 63%）；内沿圆拟合残差中位 1.75px；**内外圆心偏差中位 1.3px**——即熔核的两条边确实同心，与"熔核内沿与外沿同心于激光中心"这一物理事实一致（这正是 R21 里用户纠正我时说明的模型）。反过来该偏差也是污染指示器：两个含反射细圈残留的帧偏差达 48.8px，故设阈值 0.05×外半径，超限即不输出环宽。
  **边界（据实）**：① 环宽本轮**只作诊断量**，不参与任何判定——把它用于漏焊/炸焊会改变判据，需要重新标定阈值（未做）；② 搜索起点依赖灰圆半径，灰圆若失败则内沿也不取（此时报告内沿"未取到"，不报 0）；③ 理论上的下一步是"用熔核自己的内外两条边共同约束圆心"（用户最初的要求），实测两条边同心（偏差 1.3px）说明这一步在物理上成立且有收益空间，但本轮未做——R21 的弧段支撑择优已把圆心尾部压到 p95=61px，收益需先用全量数据评估。
  **验证**：`dotnet build` 0 警告 0 错误；单测 133 → **135 个、134 通过**（唯一失败仍是改动前即红着的 `NgSampleTests`）；新增 `RingWidthTests` 两个用例（内外半径不变式与环宽恒等式；同心门限超限时不得输出环宽）。核心指标（中心偏差中位 10.9px / p90 21.0 / 半径比 >1.25 占 0.21%）与 R21 记录一致——环宽是纯增量，未扰动既有测量。

- **R23**（灰圆粗定位尝试与回退，2026-09-16）：用户指出「圆环是挺准了，但是灰圆的识别还是非常偏」（Img023 / `19-22-43 041`）。本轮**尝试修复但失败并主动回退**，据实记录。
  **先把缺陷量化**：算法报的灰圆心 (2432.8, 1826.8) r=199.4px，而独立测量的灰盘质心 (2474.9, 1838.1) r≈96px；该 SN 24 帧的灰圆半径中位 171.7px。关键旁证：**算法报的灰圆半径 199.4px 几乎顶到熔核内沿 230.1px**，即卡尺抓到的是熔核内沿而不是灰盘的边。**同时发现一个验证盲区**：厂商 `.vdb` 里只有焊环圆的参考，**没有灰圆的**，所以此前所有"一致性 11px / p90 21px"的数字都只验证了焊环，灰圆精度从未被验证过。
  **机制**：卡尺的射线原点是**焊环圆心**，而该帧焊环圆心离灰圆心约 97px、比灰盘半径（96px）还大 → e/q≈1。在灰盘边界被反光冲掉的那一侧，"第一次超过阈值"落到了更外面的熔核内沿上；拟合平均半径因此被拉到 199px、圆心被拉向那些"跑远"的射线一侧。失效是**单侧系统性**的，离群剔除救不了。
  **两次尝试与失败原因（都有实测依据）**：
  ① **局部灰度带**（在焊环圆心取邻域估盘中位与 MAD，再按 ±k×散布 分割）：邻域实测灰度 64~255 全跨过去——焊环圆心恰好落在灰盘**边缘**上，带退化成 [0,255]、整图被选成一整块（等效半径 317px）→ 判不合格 → 回退。
  ② **"被暗缝隙包围的孤岛"拓扑判据**（在焊环内部中心区域按多档阈值找不接触搜索区边界的连通域）：**任何阈值下孤岛数都是 0**——该帧的暗缝隙只是一段**弧**、不是闭合的环，灰盘与外围底板始终连在一起。判据先决条件不成立。
  **结果**：拓扑版虽然让该 SN 的 24 帧灰圆半径中位从 171.7 降到 116.6px（说明它在多数帧上方向是对的），但**用户指出的 Img023 逐位不变**，且**单测从 134/135 掉到 97/135**（38 个失败）。两个事实合一 → **主动回退**，不把未验证的改动留在最敏感的灰圆测量上。回退后单测恢复 **135 个 / 134 通过**。
  **保留的线索**：拓扑粗定位在多数真实帧上有效（半径中位向灰盘尺寸收敛），失败的只是"缝隙不闭合"的那一类帧；因此下一步应当做**部分边界的圆拟合**（径向对称/Hough 在可见弧上求最优圆心），或先与产线确认"灰圆心"这一物理量的定义（端面几何中心 vs 可见灰区质心——两者在反光冲掉一侧边界时并不等价）。

- **R24**（P1-14 焊偏基准改为孔位，2026-09-16）：用户明确"灰色的是端面的孔位，孔里更黑的是焊环焊接时的定位中心点"，并指出"B 有时只显示圆的一部分，需要推理出全圆形来获取圆心"。本轮**完成 A（孔位）基准的落地**，B 因缺少先验未做。
  **缺陷量化（先测再改）**：Img023 上算法报的"灰圆心"(2433,1827) r=199.4px，而手工标定的端面灰盘为 (2320,1811) r≈96px；算法报的半径 199.4px **几乎顶到熔核内沿 230px**——卡尺抓到的是熔核内沿而不是孔位。根因：射线原点是焊环圆心，而该帧焊环圆心离孔心 97px（比孔半径 96px 还大），在**孔沿被反光冲掉**的一侧射线越过孔位继续外走。后果是偏移量被系统性**压低**（报 0.152mm 而真值约 0.56mm），偏小的方向正是"该判 NG 却判合格"。
  **两次失败与原因（都有实测依据）**：
  ① 用**局部灰度带**（在焊环圆心取邻域估盘中位与 MAD）：邻域灰度 64~255 全跨过去——焊环圆心恰落在灰盘边缘上，带退化成 [0,255]、整图一块 → 返回 null；
  ② 用**熔核的纹理能量阈值筛"非粗糙"再三分层**：孔位端面自身的细小斑点把它的局部标准差顶到阈值附近、被整片筛掉，剩下的 80% 都是被反光冲白的端面（255）→ 三个聚类初值（p20/p50/p80）全部塌到 255 → 返回 null。
  另外手写的三均值聚类（p20/p50/p80 初值）在**首帧就返回 null**，改为**递归 Otsu**（先在 [0,255] 求 t1 分暗/亮，再在亮侧求 t2 分孔位端面与端面金属，取 (t1,t2] 为孔位灰度区间）后正常。
  **落地**：`Vision/HoleLocator`（递归 Otsu + 圆盘形状闸门 + 半径比例闸门 + 残差闸门）；`WeldInspectionAlgorithm` 的焊偏基准改用它，梯度卡尺退为兜底；新增 5 个配置项（搜索区比例 / 孔位半径上下限 / 残差上限 / 最小层间距），全部带实测依据。
  **验证**：单测 **133/135**（2 个红：① 改动前即红的 `NgSampleTests`；② `PpSampleTests` 的"合格帧偏移量 ≤0.8mm"哨兵——修正后最大 0.937mm，但**算法判定仍 24/24 全 OK**，未超 1.0mm 公差，属哨兵过期）；tests/pp 前 20 个 SN / 480 帧：偏移量中位 0.238→0.262mm、p90 0.448→0.514mm、超 1.0mm 的帧 0→1（0.21%）；批量回归 960 帧厂商良品：**误判 NG 2 帧（0.21%）**、测量失败 1 帧。
  **残留（据实）**：① 孔位半径中位仍为 168.4px（旧 172.6px），说明**多数帧上"孔位灰盘"与"其外围光滑底板"的灰度接近、分不开**，只在灰阶可分的那部分帧（约 10%，p10 由 139 降到 104）上真正纠正到了灰盘；要普遍纠正需要形状/拓扑判据（该帧的暗缝隙只是一段弧、不闭合，拓扑法先决条件不成立，已在 R23 记录）。② B 未实现，需该标记的公称直径。③ 公差待工艺给值。

- **R25**（P1-15 现场标定与焊偏公差，2026-09-17）：用户带卡尺到现场量了**工件上的两个实物尺寸**——凹陷圆环（焊环外那条细暗圈）直径 **30mm**、孔直径 **5mm**，并明确"孔洞位于凹陷中心（两者同心）"、"凹陷圆环只在个别区域出现，只能作辅助参考，孔洞才是主基准"。以此为基准把像素当量与焊偏公差一次落地。
  **先确认这件事能当尺子用（三条独立验证）**：① 那条细暗圈是不是每帧都在的干净圆？——不是：用户那帧上只有 304/720 条射线能打到它，是**台阶阴影形成的局部弧**；其拟合残差仅 5 原图像素、灰度约 40（底板 255）、等效带宽约 0.5mm，是典型的机加工凹坑边。② 裁剪是不是 1:1？——是：低质量 JPEG 的 8×8 块边界自相关在滞后 8 处 **0.96**，与现场原图自身对照的 0.98 同样尖锐（非整数倍缩放会把这个峰抹平）；标注线宽也与 `round(base/500)` 吻合。③ 两条口径是否一致？——是：30mm 圆环占图宽 1278/5472 = 23.4% ⇒ 视野 128mm，与"42.6px/mm"完全吻合。
  **落地**：
  ① `InspectionConfig.MmPerPixel` **0.025 → 0.1003**（即 **0.0235mm/原图像素**，视野 128.4mm）。旧值配 1280 宽 = 视野 32mm，使**所有毫米读数偏小 4.01 倍**：真实的 1mm 偏移报成 0.249mm，**1.0mm 公差物理上等于 4.0mm**，这正是"该判 NG 却判合格"的量级来源。
  ② 消除像素当量的**四处各写一份**：`InspectionConfig.MmPerPixel` 只被 App/WPF 的 `DemoPlanBuilder.BuildCalibration` 消费，而 `tools/VdbImport`、`tools/Diag`、现场数据测试各自硬编码 `(1000,0)→(25,0)`——改配置当量时只有第一处跟着变，实测同一批现场图在界面里与在诊断工具/测试里报出差 4 倍的毫米数。新增 `NinePointCalibration.PureScale(mmPerPixel)` 作为唯一构造入口，工具与测试改为从配置取值。
  ③ `OffsetToleranceMm` **1.0 → 6.6**（用户："按 pp 里的那些作为正确参考来做，然后设成可调控的"）。实测（20 SN / 480 帧，全部厂商 OK，`tools/VdbImport agree` 全量跑）：min 0.198、p10 1.070、p25 1.472、**中位 1.989**、p75 2.649、p90 3.780、p95 4.091、p99 4.793、**max 5.763mm**；取 max × 1.15 = 6.6mm。界面"焊偏公差 mm"输入框本就是双向绑定 + 随 config.json 持久化（`AppConfigTests.SaveThenLoad_RoundTripsAllFields` 已覆盖），本轮补了 ToolTip 说明其来源。
  ④ `DemoPlanBuilder.BuildPlan` 的焊偏真值改由公差推出（×1.3）并夹到合成夹具可复现量程（`0.45×焊环半径 − 孔半径`，即孔位搜索区约束），不再写死 1.2~1.7mm——写死时公差一改剧本就与判定脱节。
  **验收**：`dotnet build` 0 警告 0 错误；**40 SN / 960 帧厂商良品批量回归 100% 合格**（误判 NG 0、测量失败 0）；单测 **135 个 / 131 通过**（4 个红全部是改动前即红的合成夹具用例：`WeldInspectionAlgorithmTests.ZeroOffsetPoint_IsOk(seed4)`、`RealisticRobustnessTests` ×3——本轮的改动没有新增任何失败）。A/B 验证"可调控"确实生效：把 config.json 覆盖为 `MmPerPixel=0.025, OffsetToleranceMm=1.0` 时，控制台演示精确复现旧基线 21 OK/3 NG。
  **两处据实记录的后果**：
  ① **焊偏判据在这批数据上零分辨力**：12 帧厂商 NG 的偏移量（修正当量后 1.73~5.79mm）**全部落在 6.6mm 公差内**，与良品族（0.198~5.763mm）完全重叠。`NgSampleTests` 因此从"断言检出 ≥6 张"改为"只断言测量链正常（每帧都测得出、D 落在实测带内）"并打印分布，类注释写明本批不能用来验证焊偏判据。
  ② **合成夹具的毫米尺度跟着 `MmPerPixel` 走**（`BuildCalibration` 同时服务仿真与现场四种输入源），当量 ×4 后夹具里的偏移量在像素上缩小 4 倍，演示基线由 21 OK/3 NG 变为 21 OK/2 NG/1 异常（点 20 报"孔洞半径异常 10.3%×焊环半径"，差 1.7 个百分点）。这是尺度耦合，不是算法退化——A/B 已证实。**未做**：给合成夹具自己的 mm/px 常数，把它与现场标定解耦。
  **残留与后续（据实）**：
  ① **2.0mm 中位偏移待定性**：修正当量后，这批厂商良品的"孔心↔熔核心"距离中位 1.989mm、最大 5.763mm；对激光焊而言这个量级异常大。反向证据显示**基准侧没问题**（用户那帧孔心与凹坑圆心只差 **0.12mm** = 凹坑半径的 0.83%），而**熔核侧可能被形状带偏**（该帧粗糙熔核区是新月形，质心离孔轴 3.51mm、单圆拟合 3.89mm）。分辨率办法：统计 480 帧偏移的**方向**——随机分布 = 真偏心（那公差就该由工艺给毫米级值），聚在固定方向 = 测量偏置（那要改的是"熔核中心"的取法）。**未做**。
  ② **孔位半径不可信（新证据）**：以 30mm 凹坑为尺，5mm 孔半径应为 106.5px，算法报 **131.3px（偏大 23%）**；图上可见的两条边是 79px（孔底，被阴影吃掉一圈）与 131.3px（影子外缘），真实孔沿落在阴影内部、量不到。影响：`WeldInnerSearchStartRatio = 1.15×孔位半径` 因此被放大 23%（这正是橙圈"贴合不了熔核内沿"的一个输入）。孔位**圆心**不受影响（细环已独立验证）。
  ③ `tools/Diag` **已无法编译**（其合成诊断半边引用了 P1-8 重构时删掉的配置项 `PoleMinRadiusPx`/`BackgroundKernelPx`/`SeamMinContrastPx` 等；它不在 .sln 里，故不影响 `dotnet build`，但 file 模式实际不可用，本轮改用 `tools/VdbImport agree` 完成测量）。顺手把它 file 模式的默认值改为读 `InspectionConfig`，避免又一处重复字面量。

- **R26**（用户报"还是存在误判和偏移"，2026-09-17 第二轮诊断）：用户贴出 WPF 点位详情窗口（Img011 = `21-57-37 834_..._OK_462`，厂商判 OK 却被判 `NG（焊偏）D=1.133mm`）。分两步查清：**误判的即时原因是配置文件冻结了旧标定**，而"偏移"这一半是**真的有问题，且根因已定位为固定方向的测量偏置**。
  **① 误判的即时原因：WPF 目录下的 config.json 冻结了旧值。** 该文件是应用首次运行自动落盘的快照，里面**显式**写着 `MmPerPixel: 0.025`、`OffsetToleranceMm: 1`（还冻结了 R24 之前的值：`MinRingRadiusRatio 0.09`、`MinPinRadiusRatio 0.12`）。显式值与默认值同优先级下**显式值胜出**，所以 R25 改的默认值一点都没生效——窗口里显示 `判定公差 ±1.000 mm` 就是证据。按修正当量，这一帧的真值 D = 1.133 × 4.012 = **4.55mm**，按 6.6mm 应判**合格**（该 SN 也在 R25 那 40 SN / 960 帧 100% 合格的集合里）。
  处理：把该 config.json 的 `Inspection` 段整段删掉（保留操作员的输入源与图片文件夹路径），让它按当前默认值走；并让 WPF 启动时把**生效当量**打进日志（`[标定] 生效当量 0.1003mm/标定像素 ⇒ 视野 128.4×102.7mm`），避免同类问题再静默发生。
  **② 凹坑沿给出第二条独立标定验证。** 在 Img011 上独立量那条细暗圈（工作分辨率、灰<130 的薄连通域）：拟合残差 **0.80px**、直径 **30.44mm**（现场卡尺 30mm ✓）；其圆心与算法报的**极柱心只差 0.6px = 0.11mm** —— 用户"孔洞在凹坑中心"的说法在这帧上被证实到 0.1mm，**孔位基准是可靠的**。
  **③ Img011 的红圈为什么偏：主路径本是对的，是"激进重试"把它换错了。** 复现算法两条路径：
    · **主路径**（不做薄结构删除）只出 1 个合格候选：填洞面积 25988、等效半径 **0.198×base**、圆度 0.99、支撑弧段 **1.00**，径向中位数拟合圆心 **(337.7,239.1)、离凹坑沿心 0.02mm**——也就是说**主路径给出的圆心是正确的（D≈0）**，但它这个半径是**凹坑（30mm）整体的**，落在族值带（0.142~0.18）之外；
    · 族值带外 ⇒ 触发 R21 的**激进重试阶梯**（0.045/0.060/0.075×base）：三级都给出半径落回族值带（0.160~0.164×base）的候选，但圆心分别偏离凹坑轴 **18.2/18.7/18.6px = 3.4~3.5mm**（支撑弧段 0.81~0.85，过闸门）——因为重试删掉凹坑沿薄带后，剩下的是**新月形**的熔核残块，质心被推向厚的一侧。
    · 于是"安全网"用一个**圆心量错**的候选替换掉了**圆心量对**的候选，产出自信而错误的 焊偏。
  **④ 但重试不是普遍原因——普遍原因是一个固定方向的测量偏置。** 跨帧比对（5 SN / 120 帧，Python 复现主路径的拟合圆心 vs 算法最终报的圆心）：两者**多数帧相差 <0.05mm**（中位 1.988mm vs 2.050mm），即 2mm 偏移是主路径自己量出来的，不是重试换出来的。于是查偏移**方向**（`tol_ref2.csv` 全 480 帧，仅用 CSV）：
    · 方向分布（图像坐标，30° 一档）：**88% 落在图像下半平面**（0~180°），上半平面只有 12%；
    · 平均偏移向量 **(+0.332, +1.062) mm**（+y = 图像下方），平均长度 2.168mm → **集中度 0.51**（方向高度集中，随机分布应 <0.2）；
    · **20 个 SN 每个 SN 的 24 帧平均向量都指向同一侧**（y 分量统一为正）——不同电芯/模组却给出图像坐标系里同一方向的偏移，这在物理上不可能是装配偏心；
    · 偏移方向与"孔位心指向图像中心"的余弦中位 **+0.39**（略偏向图心方向）。
  结论：**这批"焊偏"读数主要由一个固定方向（图像下方/朝向图心）的测量偏置构成，不是真实偏心**（平均 2.17mm 里有 1.11mm 是那个一致分量）。偏置的物理来源指向**纹理掩码把熔核与下方结构（台面黑边框、凹坑沿阴影带、相邻特征）非对称地并成一块**——掩码可视化已确认：被剔除的"边框"连通域横跨整幅（面积 65528、圆度 0.30），而它的一部分与熔核的并块会把质心往下方拉。
  **因此 R25 设的 6.6mm 公差此刻是在"覆盖一个测量偏置"**——它仍然是正确的筛查限（厂商良品必须全过），但**不能当成焊偏判据已可用的证据**；偏置清掉后必须重标公差（预期会从 6.6mm 掉到工艺量级）。
  **修复方向（三选一，待定）**：
  ① **把焊环搜索约束到凹坑/孔轴附近**：熔核物理上落在 30mm 凹坑之内，而孔轴每帧都有且已验证到 0.1mm——用它圈定搜索区即可一刀切掉"台面边框/下部结构"这类污染（这才是固定方向偏置的来源）。代价：可测偏心被凹坑尺寸限制（约 ≤2.4mm），需与工艺确认是否够用。
  ② **用熔核自己的内沿+外沿两条边共同约束圆心**（用户 2026-09-16 最初的要求，R21 残留里记着"本轮未做"）。最符合物理，改动最大。
  ③ 最小改法：让激进重试**拒绝圆心远离主路径候选的候选**（本帧即可修掉），但它只治"重试换错"这一种，治不了④的普遍偏置。
  本轮**未改算法**（①②③ 都会改变测量语义与公差含义，属于需用户/工艺定的范围）。

- **R27**（用户"识别失误 + 红圈偏移"第三轮，2026-09-19）：用户指定 `tests/pp/2025-09-11/Orignal_pic/SN_CCSAGZZG01A2536925090900007025080700034` 作为测试文件夹，贴出 Img000（OK，D=3.599mm）与 Img010（异常：未找到纹理显著区域）两张点位详情截图，要求优化算法直到"改善完美"。本轮**只盯这一个 SN 的 24 帧**逐帧查改，再用 40 SN / 960 帧做零回归验证。完整执行记录见 [`FIXPLAN.md`](FIXPLAN.md) 第 6 节。
  **① 先纠正了一处持续的误读**：用户截图里那条"又粗又黑的整圆"不是焊环，而是**工件上直径 30mm 的凹陷圆环**。三重实测：与孔位圆心只差 **0.38mm**（用户卡尺说过"孔在凹陷中心"）、半径 630~655 原图像素 × 0.0235mm/px = **直径 29.6~30.8mm**、且**只在部分帧成像可见**（该 SN 24 帧里只有第 0/1/10/11 帧，从孔心扫描暗带验证）；厂商 `.vdb` 里的圆与**我们的红圈**一致（差 2.1px）。所以 Img000 上"红圈比黑圈小且不同心"是**正确**成像——焊偏测的就是熔核相对孔轴的偏移。`README` 已补"怎么读标注图"。
  **② Img010 整帧零候选的根因**：熔核在**原始掩码**里是独立且不贴边的一域（bbox 178×179、最大内切半径 36.0），而 21px 闭运算（2 次迭代 ≈ 桥接 40px）把它与工件外框连成一块 88903px 的**贴边**连通域，于是"贴边 = 台面/背景"规则把熔核一起扔掉 → 候选数 0。
  **③ Img001 红圈偏小的根因**：精修的尺度 ρ 由全周中位 |∇I| 剖面给出，而熔核有一大段被镜面冲白（无梯度），ρ 被系统低估（448 vs 真值约 505）→ 窄带 [0.9,1.08]×ρ 把边界硬截在 484、圆心偏 19.4px，而且被截断的射线全落在同一半径上，把支撑弧段**假性抬高**到 0.86。
  **落地（只有两处，都是"只在既有路径失效时才生效"的加法）**：新增 `WeldRingLocator.Detect(..., preCleanBorder)`（形态学**之前**清贴边连通域）＋ `WeldInspectionAlgorithm` 的**第三路径**（前两条都给不出结果时才用，且半径必须落在 `[MinRingRadiusRatio, MaxRingRadiusRatio]` 内才采纳）；新增 `RefineBoundary` 的**外带宽截断自适应阶梯**（截断比 >0.15 才逐级放宽到 0.20/0.32，健康帧一次都不会走到第二级）。新增 3 个配置项（`EnableRingBorderCleanupFallback` / `WeldRingRefineBandOuterFallbacks` / `WeldRingRefineMaxClippedRatio`），**未改任何既有默认值**故 `CurrentConfigVersion` 不变。
  **实测**：目标 SN **23 OK + 1 异常 → 24/24 全部测出**（Img010 得 R=485.7 与厂商 481.6 差 **1.8px**；Img001 圆心偏差 19.4 → **4.6px**；其余 22 帧逐像素不变）。40 SN / 960 帧 A/B（同一份代码只切两个开关）：**932 OK / 0 NG / 28 ERR → 945 OK / 0 NG / 15 ERR**，与厂商圆心偏差中位 2.7px 不变、p90 6.4→6.5px、max 49.2px 不变——即 **13 帧由"测不出来"变成有效测量，新增误报 NG = 0**，单帧耗时 233→240ms（+3%）。外带宽阶梯的收益不止一个 SN：全量里几何变化的 7 帧中有 6 帧是"半径被截断而偏小 30~50px"，改后与厂商半径一致。
  **据实记录三处被否定的改动**（都做了实测并回退）：把候选排序改成"未填洞内切半径最大"（在目标 SN 上**零收益**且使合成夹具 1 例由"漏焊"变成"测量失败"）；"相对实心度"闸门（分离度确实够——细圈填洞块 0.02 vs 真熔核 0.9——但剔除候选会改变"谁被选中"，同样使夹具 1 例变性质，且现场无可测收益）；绝对实心度阈值 0.55（**不可移植**：现场真熔核 0.91~0.99 而合成夹具只有 0.42~0.45，因为实心度取决于"鱼鳞斑尺度 ÷ 闭运算核宽"而这个核是相对量且有 21px 绝对上限 → 夹具 31 例里 29 例失败）。
  **测试契约唯一一处更新**：`RingCredibilityGateTests.PollutedBoundaryFrame_...` 钉的正是本轮 Img010，原先断言"必须报测量失败"——那是当时**测不出来**的事实记录而非期望行为（用户两次报的都是这一帧，说明"报异常"对他就是"识别失误"）。现改为"两种结局都合法但都不许给错"：要么**圆心与厂商圆吻合**（≤30px，实测 1.8px）、半径比 0.92~1.10、支撑弧段达标，要么报测量失败且不输出偏移量；无论哪种都**不得**判成漏焊/焊偏。
  **验证**：`dotnet build` 0 error；`dotnet test` **总计 148、通过 137、失败 11**——其中 10 个是第 5.7 节记录的同一批合成夹具既有失败，第 11 个 `ImagePipelineTests.FaultyImage...` 已用"关掉本轮两个开关"的对照实验证实**与本轮无关**（失败消息逐字相同）；现场语料类（`RingCredibilityGateTests` 4/4、`PpSampleTests`、`NgSampleTests`、`RealSampleTests`、`WeldRingBoundaryTests`、`RingWidthTests`、`OffsetScaleTests`、`HoleTruthTests`、`PpLocatorAgreementTests`）全部通过。
  **残留**：仍有 15 帧（1.6%）报"测不出来"，其中 10 帧是真的成像不足（原始掩码里连一个像熔核的连通域都没有），要再救需要新机理（多尺度纹理密度 / 结构张量）；与厂商圆心偏差的尾部（p95 17px、max 49px）来自熔核本身非圆时单圆拟合的固有分歧；判定公差 6.6mm 仍是筛查限，且本轮**没有真实 NG 样本**，"新增 NG = 0"只能说明没有误报、不能说明检出能力提升。
